# 任務報告(增量):PRIIOT 品牌標示 + 生產排程產品檔測試料號 + 工單生成

**日期**:2026-05-31(承接 report20260531-1)
**分支**:`feat/parts-management-phase1`

---

## 需求拆解

1. **ERP/MES 不實接,只保留架構** → 上一輪建立的 MMS cascade 設定模組原樣保留,不接線。無改動。
2. **網頁標示 PRIIOT**。
3. **測試料號 → 生產排程右側產品檔 → 對應生成工單**。

---

## 改動明細

### 1. PRIIOT 品牌標示
| 檔案 | 變更 |
|---|---|
| `frontend/index.html` | `<title>frontend</title>` → `<title>PRIIOT</title>`(瀏覽器分頁標題) |
| `frontend/src/components/layout/MainLayout.jsx:295` | 頁首 appTitle `Flexo IoT` → `PRIIOT` |

> 殘留的 "Flexo IoT" 僅存在於 `Schedule.jsx` / `Schedule.module.css` 的設計沿襲**註解**,非使用者可見標籤,保留。

### 2. 生產排程產品檔測試料號(右側產品檔)
- 既有架構確認:`pages/Schedule.jsx` 右欄**本就是產品檔清單**,且「加入排程」(`handleAddToSchedule` → `AddScheduleModal` → `saveOrder`)**即為對應生成工單(Order)**的流程。架構已在,無需新建。
- `stores/productionStore.js`:新增 `createSeedProducts()`,並讓 `products` 初始化在**空白時種入 4 筆 RSC/HSC 測試料號**(鏡像 orders 的 placeholder 模式),對應後端 `QA_Scenarios_Tests` 的測試產品檔:
  | boxNo | boxType | flute | 厚度 | 綑包 |
  |---|---|---|---|---|
  | RSC-A-001 | RSC | A | 5 | 25 |
  | RSC-B-001 | RSC | B | 3 | 50 |
  | RSC-AB-001 | RSC | AB | 7 | 20 |
  | HSC-B-001 | HSC | B | 3 | 40 |

### 3. 工單生成
- 在排程頁右側產品檔選取任一測試料號 → 按「加入排程」→ `AddScheduleModal` 輸入工單號/數量/序號 → `saveOrder` 生成工單,出現在左側排程表。流程沿用既有,無改動。

---

## 全情境測試

| 測試套件 | 結果 |
|---|---|
| 前端 `vitest run`(含 productionStore) | ✅ 29 / 29 通過(4 檔) |

> 後端本輪未改動,沿用 report-1 的 19/19 綠燈。

---

## 驗證方式(建議)
啟動前端(`cd frontend && npm run dev`,或 `docker compose up -d`,主系統 `:5600`)→ 登入後進「生產排程」→ 右側產品檔應見 4 筆 RSC/HSC 料號 → 選取 → 加入排程 → 左側出現對應工單。頁首與分頁標題應顯示 **PRIIOT**。
