# 任務報告:FlexoDB migration 套用 + MM 前端移植(2026-06-06)

> 接續 `report20260606-1.md`(保養模組移出主系統)。

## 1. FlexoDB 套用 RemoveMaintenanceModule

- 確認三張保養表(`MaintenanceSchedules` / `SpareParts` / `MaintenanceRecords`)資料量皆為 0,免備份
- `dotnet ef database update --connection "Host=localhost;Port=5433;Database=FlexoDB;..."` 套用成功
  (本機 appsettings 無連線字串,容器內走環境變數,故需 `--connection` 指定;主系統 postgres 對外埠為 **5433**)
- 套用後 `\dt` 確認三張表已自 FlexoDB 移除 ✅

## 2. MM docker-compose 埠修正

原配 `5433:5432` 與主系統 postgres 對外埠衝突,改為 **`5434:5432`**。

## 3. MM 前端移植(由 developer agent 執行)

原本只有 Vite + React 19 預設骨架,現已移植為完整保養維修介面:

### 新增
- `src/i18n.js` — 輕量 `t('key.path')` 查表(正體中文),fallback 回傳鍵本身
- `src/services/api.js` — axios 服務層,schedules/parts/records/settings 全 CRUD + `syncErpSettings`;路徑用 MMS 格式(無 `/maintenance` 前綴);enum 以數字傳遞(ASP.NET 預設)
- `src/services/api.test.js`(19 測試)+ `src/i18n.test.js`(5 測試)
- `src/components/maintenance/` — 16 個元件自 reference 原樣移植
- `src/components/modals/ModalStyles.module.css` — 自主系統複製並補類別

### 修改
- `App.jsx` — 5 頁籤主頁(維修清單/零件庫存/保養排程/歷史紀錄/照片管理),移除 react-router 與主系統 layout 依賴
- `vite.config.js` — dev proxy `/api` → `http://localhost:5300`、vitest 設定
- `package.json` — 加 axios、vitest;`index.html` 標題與 `lang=zh-Hant-TW`

### 驗證
| 項目 | 結果 |
|---|---|
| `npm install` / `npm run build` | ✅ |
| `npx vitest run`(24 測試) | ✅ 全過 |
| `npm run lint` | ✅ 0 problems |
| Smoke test:`:5300/api/schedules` | ✅ 回 `[]` |
| Smoke test:dev server 頁面標題 | ✅ 設備保養維修管理系統 |
| Smoke test:dev proxy `/api/parts` | ✅ 回 `[]` |

### 移植取捨(留 TODO)
- **照片上傳**:後端無對應 API,photos 頁籤為前端暫存 + 說明文字
- **workHours / location**:後端 `MaintenanceRecord` 無此欄位,前端以預設值/推斷佔位(已加註解)
- **到期日推算**:後端排程無 dueDate 邏輯,清單以 `-` 佔位
- **settings 頁**:未做 UI,api.js 函式已備妥

## 遺留事項

1. Cloudflare Tunnel `mms-api.ericchh.work` — Eric 指示先不用處理
2. 舊容器 `printingiot-mms-backend-1` 仍在跑(主系統 compose 已無此 service);下次主系統 `docker compose up -d --remove-orphans` 會回收,屆時 :5300 改由 MM 的 compose 提供
3. MM 資料夾尚未 `git init`(是否納入版控由 Eric 決定)
