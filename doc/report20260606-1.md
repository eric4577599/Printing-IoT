# 任務報告:設備保養/維修模組移出主系統(2026-06-06)

## 任務概述

依 Eric 指示,將設備保養、維修功能自 Printing IoT 主系統移出,改為**獨立外掛程式**,客戶有需要時再單獨部署。獨立程式落腳於 `/Volumes/X10Pro/cusor pool/MM/`(原 repo 內 `Maintenance-System/` 整個搬遷過去)。

- 分支:`feat/extract-maintenance`(自 `feat/parts-management-phase1` 分出)
- Commit:`7a839f4`(73 檔變更,+611 / -6354)

## 異動內容

### 1. 搬遷至 MM 資料夾

| 來源 | 目的地 |
|---|---|
| `Maintenance-System/`(MMS 後端 + 前端骨架 + docs) | `/Volumes/X10Pro/cusor pool/MM/` |
| `frontend/src/components/maintenance/`(16 檔) | `MM/reference/main-frontend-maintenance/components/` |
| `frontend/src/pages/MachineMaintenance.jsx` | `MM/reference/main-frontend-maintenance/` |
| `frontend/public/help/maintenance.html` | `MM/reference/main-frontend-maintenance/` |
| (新建)獨立部署用 | `MM/docker-compose.yml`(自帶 postgres,埠 5433 避開主系統) |

### 2. 後端移除

- `PrintingIoT.Core/Entities/Maintenance/`:MaintenanceRecord、MaintenanceSchedule、SparePart
- `PrintingIoT.Core/Interfaces/IMaintenanceService.cs`
- `PrintingIoT.Infrastructure/Services/MaintenanceService.cs`
- `PrintingIoT.API/Controllers/MaintenanceController.cs`(/api/maintenance/*)
- `PrintingContext`:移除 3 個 DbSet 與 using
- `Program.cs`:移除 IMaintenanceService DI 註冊
- `PrintingIoT.Tests/Integration/QA_Maintenance_Tests.cs`
- **新增 migration** `20260606021031_RemoveMaintenanceModule`:Up drop `MaintenanceRecords` / `MaintenanceSchedules` / `SpareParts` 三張表;Down 可完整還原(含外鍵與索引)

### 3. 前端移除

- `pages/MachineMaintenance.jsx`(348 行)
- `components/maintenance/` 全目錄(16 檔)
- `services/api.js`:6 個保養 API 函式
- `App.jsx`:lazy import 與 `/maintenance` 路由
- `MainLayout.jsx`:導覽列「保養維修」項目
- `HelpModal.jsx`:說明項目(後續項目重新編號)
- `LanguageContext.jsx`:tw / zh-CN / en / vi / th 五語系的 `nav.maintenance` 與 `maintenance.*` 區塊
- `locales/tw.js`:同上
- `public/help/maintenance.html`

### 4. docker-compose

- 移除 `mms-backend` service(原埠 5300、MmsDB)
- DEPRECATED 註解區改記錄 EXTRACTED 事項與 MM 新位置

## 明確保留(易混淆項目)

| 項目 | 原因 |
|---|---|
| `modules/maintenance/MaintenancePage.jsx` | 名稱誤導,實為訂單排程/產品庫頁面 |
| `Part` / `Supplier` / `SupplierPart` 零件管理 | 屬 parts-management 範圍,與保養專用 SparePart 是兩套 |
| `MachineStatus.Maintenance = 4` 列舉 | 機台狀態,非保養模組 |
| `orders.products.maintenance_title`(資料庫維護) | 產品庫的資料庫維護功能 |
| vi/th 語系 `settings.tabs.maintenance` 殘留鍵 | 無程式引用,非本模組,維持最小變更 |

## 驗證結果

| 項目 | 結果 |
|---|---|
| 後端 `dotnet build` | ✅ 0 錯誤 0 警告 |
| 後端 `dotnet test` | ✅ 17/17 通過 |
| 前端 `npm run build` | ✅ 2.23s 完成,bundle 無 maintenance chunk |
| MM 端 `dotnet build`(MaintenanceSystem.sln) | ✅ 0 錯誤,可獨立建置 |
| 殘留引用 grep 檢查 | ✅ 無 |

## 待辦/後續事項

1. **Cloudflare Tunnel**:`mms-api.ericchh.work` 的路由仍指向 docker 網路內的 `mms-backend:5300`,主系統 compose 已無此 service;待 MM 獨立部署後需在 Cloudflare dashboard 重新指向(或暫時停用該 hostname)
2. **資料庫**:migration 尚未 `dotnet ef database update` 套用到正式 FlexoDB;套用前若需保留既有保養資料,先行匯出
3. **MM 前端**:目前僅 Vite 骨架,可參考 `MM/reference/main-frontend-maintenance/` 的元件補齊功能
4. 移除是在 `feat/extract-maintenance` 分支,`BoxDiagram.jsx` 未提交修改仍留在工作區(屬 parts 分支範圍)

## 沙箱注意事項

`dotnet build` / `dotnet ef` 在 Claude Code 沙箱內會以「建置失敗(0 錯誤)」失敗,需停用沙箱執行(與既有記憶「測試需停沙箱」一致)。
