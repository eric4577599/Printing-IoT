# 任務報告 — Cloudflare Tunnel 斷線診斷與修復

> 日期:2026-06-11
> 分支:`feat/extract-maintenance`
> Commit:`9a0989c fix(deploy): cloudflared 強制 http2 協議,修復 QUIC 反覆斷線`

## 問題症狀

對外網址(`priiot.ericchh.work`)間歇性打不開;使用者重開 stack 後要求重新檢查。

## 診斷過程

1. **本機健檢**:8 個容器、API(Swagger)、兩個前端、DB migration 全部正常 → 排除程式碼問題
2. **重開後發現**:
   - `sm-frontend` 容器 Exited(0) 未被拉起 → `docker compose up -d sm-frontend` 修復
   - cloudflared `connIndex=2` 以 QUIC 協議反覆斷線重連:`control stream encountered a failure`、`timeout: no recent network activity` → **根因:本地網路 UDP/QUIC 不穩**

## 修復內容

| 項目 | 動作 | 結果 |
|---|---|---|
| cloudflared 協議 | `docker-compose.yml` 加 `--protocol http2`(改走 TCP) | 4/4 連線穩定註冊,無錯誤 |
| cloudflared 版本 | `docker compose pull`(2026.3.0 → 最新) | 同步消除過舊警告 |
| sm-frontend | 重新啟動 | `:5100` 回 200 |

## 端對端驗證(公網)

| 網址 | 結果 |
|---|---|
| `https://priiot.ericchh.work/` | 200 ✅ |
| `https://priiot.ericchh.work/api/Monitor/realtime` | 204 ✅(無即時資料屬正常) |
| `https://priiot.ericchh.work/api/v1/Parts` | 200 ✅ |
| `https://smartparts.ericchh.work/` | 403(Cloudflare Access 保護,未登入屬預期) |
| `https://mms-api.ericchh.work/` | 404 ❌(殘留路由,見下) |

## 待處理:Cloudflare Dashboard 殘留路由

Tunnel ingress 設定(remotely-managed,存在 Dashboard 不在 repo)有兩條壞規則:

1. `mms-api.ericchh.work → http://mms-backend:5300` — **mms-backend 已於 2026-06-06 拆出主系統**(獨立 MM 程式),此路由必死,建議刪除
2. `smartparts.ericchh.work → http://frontend:5600` — 重複 hostname(第二條永遠不會命中)且 port 錯誤(容器聽 80),建議刪除

處理位置:Cloudflare Zero Trust → Networks → Tunnels → 該 tunnel → Public Hostname。

## 結論

問題在**部署層**(QUIC 不穩 + 容器未拉起 + Dashboard 殘留路由),非程式碼問題。主系統與零件管理程式經公網端對端驗證皆正常運作。
