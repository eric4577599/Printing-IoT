# S4 驗收報告 · 報表與分析四頁改讀後端 API

- **規格**:`docs/spec20260905-s4-v1.md`(v1,2026-09-05)
- **驗收日期**:2026-09-05
- **驗收角色**:Tester(獨立驗收,不採信 RD 的自述,所有結論皆親自執行取得)
- **結論**:**FAIL**,卡在 **T2 semantic**,退回 **RD**
- **一句話**:T1 全綠(263 通過、lint 零新增、DoD 4 grep 無結果),功能面對得上規格;但規格明文要求的兩項交付物未落地 —— 決策 4 要求寫進 `reportUtils` 的 SSOT 分界註解,以及 §11 DoD 第 5 項的 `docs/report20260905-{序號}.md` 交付報告。

---

## T1 · deterministic(自動化測試與 build)— **PASS**

### T1-1 測試套件(DoD 1)

親自執行 `cd frontend && npm run test`:

```
 Test Files  1 failed | 24 passed (25)
      Tests  263 passed | 1 skipped (264)
   Duration  3.37s
```

唯一失敗的 suite 是 `frontend/tests/dashboard.e2e.test.js`(puppeteer),失敗原因為

```
Error: The browser is already running for /tmp/claude-501/puppeteer_dev_chrome_profile-HLerhC.
```

屬環境層級(Chrome profile 佔用),且規格 §9 明文「`frontend/tests/dashboard.e2e.test.js`(puppeteer)不修、不視為回歸」,故豁免。

對照 DoD 1 基準線:

| 項目 | 基準線(規格 §11) | 本輪實測 | 判定 |
|---|---|---|---|
| 測試檔 | 19 個,18 通過 | 25 個,24 通過 | +6 檔,豁免檔仍是唯一失敗 |
| 測試數 | 190 通過、1 略過 | 263 通過、1 略過 | +73 測試 |
| 既有測試轉紅 | — | **0** | ✅ |

**T1-1 通過**:總數 = 基準 + 本輪新增,既有測試零轉紅。

### T1-2 Lint(DoD 2)

親自執行 `cd frontend && npm run lint`:

```
✖ 69 problems (51 errors, 18 warnings)
```

基準線為 51 errors / 18 warnings,**完全相同,零新增**。剩餘 error 皆落在本輪未觸碰的既有檔(`orderMapper.js:42`、`productionStore.test.js:6`、`dashboard.e2e.test.js:36` 等)。**T1-2 通過**。

### T1-3 DoD 4 · 兩個入口不再直讀 localStorage

```
$ grep -rn "localStorage.getItem('productionHistory')" \
    frontend/src/modules/reports/ReportsPage.jsx \
    frontend/src/pages/analysis/hooks/useAnalysisData.js
(無輸出,exit=1)
```

**T1-3 通過**。儲存層存取已集中到 `frontend/src/hooks/useProductionRecords.js:37`(`readLocalRecords`)與 `:59`(`patchLocalProductionRecord`),呼叫端不再直接碰 `localStorage`。

### T1-4 後端

本輪只動前端(規格 §10 第 5 點),未變更任何 `.cs` 或 migration,無 `dotnet build` 需求。已確認 `backend/PrintingIoT.Tests/` 無新增或修改。

> **T1 判定:PASS**

---

## T2 · semantic(程式邏輯是否符合規格描述的行為)— **FAIL**

### T2-A 對得上的部分(逐項讀碼確認)

先記錄查核通過的項目,讓下一輪 RD 知道**不需要重做**這些:

| 規格條目 | 實作位置 | 查核結果 |
|---|---|---|
| F1.1 對映表逐欄 | `frontend/src/utils/completionMapper.js:110-147` | ✅ 逐項對應;`orderNo` 走 `|| '-'`(E-09)、`date` 走 `toDateOnly` 截前 10 碼(E-18) |
| F1.1 後端率值不重算 | `completionMapper.js:132-136` | ✅ 四個率值僅 `num()` 轉型,無 `calculateOEE` 介入 |
| F1.2 `duration` 轉 `"MM:SS"` | `completionMapper.js:19-29` | ✅ 分不封頂、秒進位到 60 改 `mm+1`/`00`(E-07、E-08) |
| F1.2 `time` 為 null 回 `'-'` | `completionMapper.js:37-42` | ✅ 另擋 `Invalid Date` |
| F1.3 本機列率值重算 | `completionMapper.js:165-183` | ✅ 以 `calculateOEE` 覆寫四欄;`clientRecordId` 補 `String(id)`;`date` 缺失以 `finishedAt` 補、兩者皆缺回 `null` |
| F1.4 去重後端勝出 | `completionMapper.js:219-253` | ✅ 空鍵不參與去重一律保留(E-13);同鍵本機重複留先出現者(E-12);排序 `date` 遞減再 `finishedAt` 遞減 |
| F2 分頁演算法 | `frontend/src/services/api.js:183-205` | ✅ 短頁終止、`page === maxPages` 才標 `truncated`、非陣列 break(E-04)、超滿頁繼續(E-05)、任一頁拋錯整體拋出(不吞例外) |
| F3 hook 契約 | `useProductionRecords.js:86-143` | ✅ 七個回傳欄位齊全;失敗走降級不重拋(`:129-135`);競態以 `requestIdRef` 序號擋(`:113`、`:120`);卸載以 `mountedRef` 擋(`:98-105`);快取解析失敗 `console.warn` 當空陣列(`:42-45`) |
| F4.1 `fetchRange` 依分頁 | `ReportsPage.jsx:82-98` | ✅ 四個分頁各自對應;月報/停車回報前退回 `appliedRange` |
| F4.1 手動上傳阻擋後端列 | `ReportsPage.jsx:143-146`、`:157-161` | ✅ 開窗與存檔雙重守;存檔後 `reload()`(`:165`) |
| F4.1 CSV 多一欄資料來源 | `ReportsPage.jsx:175`、`:180` | ✅ |
| F4.2 `useAnalysisData` 加法式 | `useAnalysisData.js:71-91` | ✅ 既有回傳欄位一個不少,只新增 6 個 |
| F4.3 補欄位策略 | `completionMapper.js:193-206` + `useProductionRecords.js:122-125` | ✅ 以 `clientRecordId` 建 Map,查不到回 `'-'` |
| F5 i18n 五語系 | `LanguageContext.jsx` | ✅ `backendRecordReadOnly` 等新鍵在 tw/cn/en/vn/th 各 1 份;`localeParity.test.js` 通過 |
| F6 補送 `startedAt` | `Dashboard.jsx:546`(`startedAtIso`)、`:432`(payload `startedAt`) | ✅ 加法式,未動既有 `time` 顯示字串 |
| §5 狀態呈現 | `ReportsPage.jsx:206-240`、`AnalysisPage.jsx:246-273` | ✅ 主狀態三者互斥、警示列三條可疊加、明細表以 `styles.localRow` + 來源欄雙重標示 |
| 驗收標準覆蓋 | — | ✅ AC-S4-01 ~ AC-S4-32 **32 條全部有對應測試**,且皆為可證偽寫法(見下) |

**測試可證偽性抽查**(避免「有測試但等於沒測」):

- `completionMapper.test.js:225-234`(AC-S4-19)先斷言 `calculateOEE(...)` 由同組數字得 `72.9`,再斷言後端列仍是刻意設定的 `12.3` —— 若實作重算,此測試必紅。真檢查。
- `completionMapper.test.js:186-196`(AC-S4-18)同時斷言 `n.oee === 72.9` 與 `n.oee !== 99`,雙向釘住。
- `ReportsPage.test.jsx` 只 mock `axios`,走真正的 `ReportsPage` → `useProductionRecords` → `api`,不是重述實作的假測試。
- `useProductionRecords.test.js:227-251`(E-14)先 resolve 新請求再 resolve 舊請求,斷言結果為 `NEW-ORDER` —— 真的抓得到競態。

### T2-B 未達成項目(FAIL 原因)

#### ❌ 缺失 1 · 決策 4「避免漂移的紀律」第 1 條未落實

規格 §3 決策 4 明文寫著(並特別註記「**必須落實成程式碼,不是口頭承諾**」):

> `reportUtils.calculateOEE` / `calculateUtilization` 的函式註解必須標明:「本函式是 `PrintingIoT.Core.Services.OeeCalculator` 的 JS 孿生,規則見 `docs/spec20260903-s3-v1.md` §5.1;**僅用於本機列與彙總重算,後端列的率值一律採用後端回傳值**。」

實測:

```
$ git status --short frontend/src/utils/reportUtils.js
(空 —— 本檔本輪從未被修改)

$ grep -n "孿生\|僅用於本機列\|後端列的率值一律採用" frontend/src/utils/reportUtils.js
NOT FOUND
```

現況:

- `frontend/src/utils/reportUtils.js:184-185`(`calculateOEE` 的註解)只有 S3 時期就存在的「與後端 `PrintingIoT.Core.Services.OeeCalculator` 是同一份規則的兩處實作」,**缺少決策 4 新增的後半句**(誰是 SSOT、後端列不得重算)。
- `frontend/src/utils/reportUtils.js:157-164`(`calculateUtilization` 的註解)**完全沒有提到 `OeeCalculator`**,只寫了「§5.1 規則 2」。

同一條紀律的第 2 個 bullet(對映層註解)**已完成**,見 `completionMapper.js:5` 與 `:107-108`,故缺的只有 `reportUtils` 這一側 —— 而這正是決策 4 想防的漂移落點:未來只看 `reportUtils` 的人不會知道後端列禁止重算。

#### ❌ 缺失 2 · DoD 第 5 項交付報告不存在

規格 §11 DoD 第 5 項要求交付報告寫入 `docs/report20260905-{序號}.md`,並**必須明列五項內容**(不做回填遷移、`reportUtils` 不退場及 SSOT 分界、`customer`/`productName` 靠本機快取補、既有停機起始時間仍為 `'-'`、§8 backlog 逐項)。

實測:

```
$ ls docs/report20260905*
(eval):1: no matches found: docs/report20260905*
```

`docs/` 內最新的交付報告是 `report20260903-2.md`。本輪未產出;`git status` 的未追蹤清單也只有 `docs/spec20260905-s4-v1.md`,沒有 report。

> **T2 判定:FAIL** —— 兩項皆為規格明文要求(一項標了「必須落實成程式碼」、一項列在 DoD),且原因是實作漏做而非規格有歧義,故 **route = RD**。

---

## T3 · judgment(驗收契約合理性)— **未執行**

依驗收流程,T2 已 FAIL 即停止,不往下檢查。

僅記錄一項 T2 過程中發現、**不影響本次判定**的觀察,供下一輪參考(不列為失敗項):

- 規格 F3 步驟 2 寫「先讀本機快取並以 `filterByDateRange` 過濾到 `[from, to]`,再 `normalizeLocalRecord`」,實作 `useProductionRecords.js:40-41` 是**先正規化再過濾**。此順序差異是 RD 刻意為之並已在 `:32-33` 註解說明理由:`normalizeLocalRecord` 會以 `finishedAt` 補 `date`,若先過濾,只有 `finishedAt` 而無 `date` 的紀錄會被誤殺。實作順序的結果集是規格順序的正確超集,判定為改善而非偏差,`AC-S4-20` 亦已釘住區間邊界。下一輪可考慮把此順序回寫規格。

---

## 修正指引(給下一輪 RD)

1. **`frontend/src/utils/reportUtils.js`** —— 為 `calculateUtilization`(第 157-165 行的註解區塊)與 `calculateOEE`(第 173-192 行的註解區塊)各補上決策 4 指定的字句,重點是「僅用於本機列與彙總重算,**後端列的率值一律採用後端回傳值**」這一句必須出現。該檔既有註解使用全形括號與全形逗號,新增文字沿用該檔風格即可;此改動不影響任何測試。
2. **`docs/report20260905-1.md`** —— 依 DoD 第 5 項產出交付報告,五項內容缺一不可。建議一併寫入 E-11 的限制提醒(本機快取超過 1000 筆截斷後,最舊資料無法補救),規格 E-11 明文要求「交付報告須提醒此限制」。
3. 上述兩項完成後,功能面無需再動 —— T2-A 表列的 17 項與 AC-S4-01 ~ 32 已全數通過;重跑 `npm run test` 與 `npm run lint` 確認未回歸即可。
