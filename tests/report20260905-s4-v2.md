# S4 驗收報告 v2 · 報表與分析四頁改讀後端 API

- **規格**:`docs/spec20260905-s4-v1.md`(v1,2026-09-05)
- **前一輪**:`tests/report20260905-s4-v1.md`(FAIL / T2 / route=RD,兩項規格明文交付物未落地)
- **驗收日期**:2026-09-05
- **驗收角色**:Tester(獨立驗收;所有結論皆親自執行取得,不採信 RD 自述,亦不沿用 v1 報告的結論而重新複查)
- **結論**:**PASS**(failedLayer=NONE、route=NONE)
- **一句話**:v1 卡住的兩項(`reportUtils` SSOT 註解、DoD 第 5 項交付報告)已補齊;T1 全綠、T2 逐條複查對得上規格、T3 驗收契約合理且 edge case 覆蓋充分,三層皆通過。

---

## 0. 本輪相對 v1 的變動範圍

```
$ git status --short
 M frontend/src/utils/reportUtils.js        ← v1 缺失 1 的修正(僅註解)
?? docs/report20260905-1.md                 ← v1 缺失 2 的補件
(其餘 12 個 M / 7 個 ?? 與 v1 驗收時相同)
```

功能程式碼**零變動**(`git diff` 顯示 `reportUtils.js` 只有 6 行新增 / 2 行刪除,全在 JSDoc 註解區塊內),因此 T2 的功能面仍重新複查一次以確認未回歸。

---

## T1 · deterministic(自動化測試與 build 是否實際跑過且通過)— **PASS**

### T1-1 測試套件(DoD 1)

親自執行 `cd frontend && npm run test`:

```
 RUN  v4.1.4 /Volumes/G70Pro/cusor pool/Printing IoT/frontend

 Test Files  1 failed | 24 passed (25)
      Tests  263 passed | 1 skipped (264)
   Duration  2.99s
```

唯一失敗的 suite 是 `frontend/tests/dashboard.e2e.test.js`(puppeteer):

```
Error: The browser is already running for /tmp/claude-501/puppeteer_dev_chrome_profile-6FcTXo.
Use a different `userDataDir` or stop the running browser first.
 ❯ tests/dashboard.e2e.test.js:14:19
```

失敗點在 `puppeteer.launch`(Chrome profile 佔用),屬環境層級、與本輪程式碼無關;且規格 §9 明文「`frontend/tests/dashboard.e2e.test.js`(puppeteer)不修、不視為回歸」,故豁免。

對照 DoD 1 基準線:

| 項目 | 基準線(規格 §11) | 本輪實測 | 判定 |
|---|---|---|---|
| 測試檔 | 19 個,18 通過 | 25 個,24 通過 | +6 檔;唯一失敗仍是豁免檔 |
| 測試數 | 190 通過、1 略過 | 263 通過、1 略過 | +73 測試 |
| 既有測試轉紅 | — | **0** | 通過 |

**T1-1 通過**:測試總數 = 基準 + 本輪新增,既有測試零轉紅。

### T1-2 Lint(DoD 2)

親自執行 `cd frontend && npm run lint`:

```
✖ 69 problems (51 errors, 18 warnings)
```

基準線為 51 errors / 18 warnings,**數字完全相同,零新增**。尾端列出的 error 皆落在本輪未觸碰的既有檔(`orderMapper.js:42`、`productionStore.test.js:6`、`dashboard.e2e.test.js:36`、`api.test.js:42/55`)。**T1-2 通過**。

### T1-3 DoD 4 · 兩個入口不再直讀 localStorage

```
$ grep -rn "localStorage.getItem('productionHistory')" \
    frontend/src/modules/reports/ReportsPage.jsx \
    frontend/src/pages/analysis/hooks/useAnalysisData.js
exit=1  (無輸出)
```

**T1-3 通過**。儲存層存取已集中到 `frontend/src/hooks/useProductionRecords.js:37`(`readLocalRecords`)與 `:59`(`patchLocalProductionRecord`),兩個入口的呼叫端不再直接碰 `localStorage`。

### T1-4 後端與相依套件

```
$ git diff --stat frontend/package.json frontend/package-lock.json backend/
(空)
```

本輪只動前端(規格 §10 第 5 點),`backend/` 零變更、無 migration、無 `dotnet build` 需求;`package.json` / `package-lock.json` 未動,符合規格 §9「不新增任何 NuGet 或 npm 相依套件」。**T1-4 通過**。

> **T1 判定:PASS**

---

## T2 · semantic(程式邏輯是否符合規格描述的行為)— **PASS**

### T2-1 v1 兩項失敗的修正驗證

#### v1 缺失 1 · 決策 4「避免漂移的紀律」第 1 條 —— **已修正**

規格 §3 決策 4 要求 `calculateOEE` / `calculateUtilization` 的函式註解必須標明 SSOT 分界。實測 `git diff frontend/src/utils/reportUtils.js`:

`frontend/src/utils/reportUtils.js:166-167`(`calculateUtilization`,原本完全沒提 `OeeCalculator`):

```
 *              本函式是 PrintingIoT.Core.Services.OeeCalculator 的 JS 孿生,規則見
 *              docs/spec20260903-s3-v1.md §5.1;僅用於本機列與彙總重算,後端列的率值一律採用後端回傳值。
```

`frontend/src/utils/reportUtils.js:187-189`(`calculateOEE`,原本只有 S3 時期的「同一份規則的兩處實作」):

```
 * @description 本函式是 PrintingIoT.Core.Services.OeeCalculator 的 JS 孿生,規則見
 *              docs/spec20260903-s3-v1.md §5.1,兩處實作由測試釘住相同數值;
 *              僅用於本機列與彙總重算,後端列的率值一律採用後端回傳值。
```

兩處皆含規格指定的關鍵句「**僅用於本機列與彙總重算,後端列的率值一律採用後端回傳值**」。改動純為 JSDoc,未觸及任何可執行程式碼(diff 6 增 2 刪,全在註解區塊內),`reportUtils.test.js` 未修改且仍全綠(AC-S4-32 不受影響)。同一條紀律的第 2 個 bullet(對映層註解)在 v1 已完成,見 `completionMapper.js:1-8` 檔頭與 `:107-108` 函式註解。**已修正**。

#### v1 缺失 2 · DoD 第 5 項交付報告 —— **已修正**

```
$ ls -la docs/report20260905-*.md
-rw-r--r--  1 hezhaoxing  staff  17464  9月  5 14:44 docs/report20260905-1.md
```

逐條核對 DoD 第 5 項要求「必須明列」的五件事:

| DoD 要求 | 落點 | 查核 |
|---|---|---|
| 本輪不做資料回填遷移,舊資料以「合併呈現 + 去重」處理 | `docs/report20260905-1.md` §3.1 | 通過。明寫後端只有 S3 切換後的新完工,並補上「合併呈現繞過了問題,不代表問題已解」 |
| `reportUtils` 本輪不退場及理由 + 「單筆率值 SSOT 在後端、彙總 SSOT 在前端」分界 | §3.2 | 通過。含六列 SSOT 對照表,並回指註解已落實成程式碼 |
| `customer` / `productName` 靠本機快取補,換機器或超過 1000 筆會顯示 `'-'` | §3.3 | 通過。三項限制逐條列出 |
| 既有停機明細起始時間仍為 `'-'`,F6 只修往後的新資料 | §3.4 | 通過 |
| §8 backlog 逐項 | §3.5 | 通過。六項齊全且逐項附不做的理由 |
| (規格 E-11 額外要求)交付報告須提醒本機快取 1000 筆截斷的限制 | §3.3 末段 | 通過。且誠實指出「若那些紀錄產生於 S3 切換之前,整筆紀錄本身也一併消失,本輪的合併呈現救不回來」 |

**已修正**。

### T2-2 功能面逐項複查(本輪重新讀碼,非沿用 v1 結論)

| 規格條目 | 實作位置 | 查核結果 |
|---|---|---|
| F1.1 對映表逐欄 | `frontend/src/utils/completionMapper.js:110-147` | 通過。`orderNo` 走 `|| '-'`(E-09);`date` 走 `toDateOnly` 截前 10 碼(E-18);`prepTime`/`runTime`/`stopTime` 對到 `*Minutes` 欄位 |
| F1.1 後端率值不重算 | `completionMapper.js:132-136` | 通過。四個率值只經 `num()` 轉型,該檔對 `calculateOEE` 的唯一呼叫在 `:165`(`normalizeLocalRecord`),後端路徑不經過 |
| F1.2 `duration` 轉 `"MM:SS"` | `completionMapper.js:19-29` | 通過。`3.5 → '03:30'`;分不封頂(E-07);`ss >= 60` 進位成 `mm+1`/`'00'`(E-08);負值與非數字夾到 0(E-06) |
| F1.2 `time` 為 null 回 `'-'` | `completionMapper.js:37-42` | 通過。另以 `Number.isNaN(d.getTime())` 擋 `Invalid Date`(AC-S4-23) |
| F1.3 本機列率值重算 | `completionMapper.js:159-184` | 通過。以 `calculateOEE` 覆寫四欄;`clientRecordId` 補 `String(id)`;`date` 缺失以 `finishedAt` 補、兩者皆缺回 `null` 由呼叫端濾掉 |
| F1.4 去重後端勝出 | `completionMapper.js:219-253` | 通過。`backendKeys` 命中即捨棄本機列(`:235`);同鍵本機重複留先出現者(`:237`,E-12);空鍵不參與去重一律保留(`:233` 的 `if (key)` 守衛,E-13);排序 `date` 遞減再 `finishedAt` 遞減(`:246-250`) |
| F2 分頁演算法 | `frontend/src/services/api.js:183-205` | 通過。短頁終止(`:196`);僅 `page === maxPages` 才標 `truncated`(`:201`);非陣列 `break`(`:192`,E-04);`data.length > pageSize` 不判定取完而續抓(E-05);任一頁拋錯整體拋出、不吞例外(無 try/catch) |
| F3 hook 契約 | `hooks/useProductionRecords.js:86-143` | 通過。七個回傳欄位齊全(`:142`);失敗走降級不重拋(`:129-135`);競態以 `requestIdRef` 序號擋(`:113`、`:120`、`:130`);卸載以 `mountedRef` 擋(`:98-105`);快取解析失敗 `console.warn` 當空陣列(`:42-45`,AC-S4-15) |
| F4.1 `fetchRange` 依分頁 | `ReportsPage.jsx:82-98` | 通過。四個分頁各自對應規格 §4 的表;月報/停車回報之前退回 `appliedRange`,不會抓到 `undefined` 區間 |
| F4.1 手動上傳阻擋後端列 | `ReportsPage.jsx:143-147`、`:158-163` | 通過。`handleOpenUpload` 與 `handleSaveUpload` 雙重守;存檔後 `reload()`(`:167`),不只改本地 state |
| F4.1 CSV 多一欄資料來源 | `ReportsPage.jsx:175`、`:180` | 通過。表頭與資料列同步加,且走 `t()` 非硬編碼 |
| F4.2 `useAnalysisData` 加法式 | `useAnalysisData.js:25-35`、`:78-91` | 通過。既有回傳欄位(`productionHistory`/`groupedData`/`summaryStats`/各 setter/`t`)一個不少,只新增 6 個;`groupedData`、`summaryStats` 計算邏輯未動 |
| F4.3 補欄位策略 | `completionMapper.js:193-206` + `useProductionRecords.js:122-125` | 通過。以 `clientRecordId` 建 Map、查不到回 `'-'`(不是 `undefined`、不是空字串) |
| F5 i18n 五語系 | `LanguageContext.jsx` | 通過。`reportView.state.*` / `reportView.source.*` / `reportView.col.source` / `reportView.btn.retry` / `reportView.alert.backendRecordReadOnly` / `analysis.state.*` 皆新增,`localeParity.test.js`(斷言五語系葉鍵集合與 `tw` 完全相同且值皆非空字串)全綠 |
| F6 補送 `startedAt` | `Dashboard.jsx:432`、`:546` | 通過。加法式,未動既有 `time` 顯示字串 |
| §5 狀態呈現 | `ReportsPage.jsx:206-240`、`AnalysisPage.jsx:243-272` | 通過。主狀態三者條件互斥(`isLoading` / `!isLoading && isDegraded && !hasRecords` / `!isLoading && !error && !hasRecords`),與規格 §5 表格逐條相符;警示列三條可疊加;明細表以 `styles.localRow` 列樣式 + 來源欄雙重標示 |
| §9 紅線 | — | 通過。`package.json` / `package-lock.json` / `backend/` 零變更;無新增 `[Authorize]`;無 docker-compose 變更;`reportUtils` 既有 export 一個未刪 |
| 專案紅線(中國用語) | 本輪新增/修改檔 | 通過。掃「接口/優化/程序/提交/默認/軟件/信息」等詞於七個主要檔案,無命中 |

### T2-3 無限迴圈風險複查(§1.2 的坑,本輪最容易出錯處)

`MonthlyReportView.jsx:48-53` / `StopReasonView.jsx:32-37` 的 `useEffect` 依賴各自的區間與 `onRangeChange`;父層 `ReportsPage.jsx:66-79` 的兩個 handler 以 `useCallback([])` 保持參考穩定,且在值相同時回傳 `prev` 物件不觸發 setState。兩層都擋住了「回報 → 重繪 → 再回報」的迴圈。實測 AC-S4-06 測試中切分頁後呼叫次數穩定(`ReportsPage.test.jsx:132-147` 斷言班別變更後 `mockInstance.get` 呼叫次數不變),可反證未發生失控重抓。

### T2-4 驗收標準覆蓋與可證偽性

```
$ grep -rno "AC-S4-[0-9]*" frontend/src/tests/ | sort -u -V
AC-S4-01 … AC-S4-32   (32 條全部命中,無遺漏)
```

分佈於 8 個測試檔:`utils/completionMapper.test.js`、`services/api.test.js`、`hooks/useProductionRecords.test.js`、`hooks/useAnalysisData.test.jsx`、`components/ReportsPage.test.jsx`、`components/ReportRangeCallback.test.jsx`、`components/AnalysisPageState.test.jsx`、`i18n/analysisKeys.test.js`。

抽查可證偽性(避免「有測試但等於沒測」):

- `completionMapper.test.js:225-234`(AC-S4-19):先斷言 `calculateOEE(...)` 由同組數字得出的值,再斷言後端列仍是刻意設定的不同數字 —— 若實作改成重算,此測試必紅。真檢查。
- `completionMapper.test.js:186-196`(AC-S4-18):同時斷言 `n.oee === X` 與 `n.oee !== 99`(舊公式殘值),雙向釘住。
- `api.test.js`(AC-S4-08):斷言 `toHaveBeenCalledTimes(20)` 恰為 `maxPages`,加上 AC-S4-08b 以 `maxPages: 3` 反證上限確實可控 —— 若迴圈條件寫錯會直接卡住或次數不符。
- `ReportsPage.test.jsx` 只 mock `axios`,走真正的 `ReportsPage → useProductionRecords → api` 全鏈路,不是重述實作的假測試;AC-S4-28 實際點月份下拉再斷言請求 params,E-16 以 `Storage.prototype.setItem` spy 斷言「不寫任何儲存層」。
- `useProductionRecords.test.js:228-252`(E-14):先 resolve 新請求再 resolve 舊請求,斷言結果為新區間資料 —— 真的抓得到競態,不是形式測試。

> **T2 判定:PASS**

---

## T3 · judgment(驗收契約本身是否合理、是否有 edge case 沒被規格涵蓋)— **PASS**

### T3-1 驗收標準本身的合理性

| 檢查點 | 判定 |
|---|---|
| AC 是否可證偽(而非「有跑就算過」) | 通過。32 條皆綁定具體數值或呼叫次數斷言;AC-S4-19 / AC-S4-18 甚至以「刻意不同的數字」設計成只要實作走錯就會紅 |
| AC 是否覆蓋規格的全部功能需求 | 通過。F1~F6 逐項都能對到至少一條 AC;F6 雖無獨立 AC,但由 AC-S4-23 從讀取端釘住 `null → '-'`,寫入端為兩行加法式 payload,風險可接受 |
| DoD 判準是否客觀 | 通過。四條全是可執行指令(測試數、lint 數字、grep 無結果、報告存在與內容),不含「看起來沒問題」這類主觀判準 |
| 是否有 AC 互相矛盾 | 無。AC-S4-03(後端 oee 勝出)與 AC-S4-18(本機列重算)分屬不同來源的列,由 `source` 明確分流,不衝突 |
| 是否有靜默失敗的缺口 | 已由規格自己堵住。最危險的一處(`duration` 塞數字會讓停機彙總全變零而不拋例外)由 AC-S4-22 以往返一致性釘住,實測 `formatDurationMMSS(3.5)` → `'03:30'` → `durationToMinutes` → `3.5` |
| 降級是否看得見 | 通過。規格 §5 把「錯誤」與「無資料」的措辭強制分開,並由 AC-S4-29 以不同 i18n 鍵斷言 —— 這正是「查詢失敗不可看起來像沒生產」這條真正重要的驗收契約 |

### T3-2 規格未涵蓋的 edge case(逐項評估是否構成阻擋)

規格 §7 已列 18 條 edge case,覆蓋度高於一般水準。本輪額外找到三處規格未明說的情形,逐項評估後**均判定為不阻擋**,列此供 backlog 參考:

| # | 情形 | 現行行為 | 為何不阻擋 |
|---|---|---|---|
| O-1 | **工廠日 vs 本機日界線落差**:後端 `productionDate` 依 `Factory:DayBoundaryHour` 換算,本機列的 `date` 是當初寫入時的日曆日。跨日界的同一筆完工,兩者可能差一天 | 若查詢區間只涵蓋本機日而不含工廠日,後端不會回這筆,該本機列會被算進 `localOnlyCount` 並標示「尚未同步至後端」,即使它其實已同步 | 只影響**標示措辭**,資料本身照常顯示、不重複、不遺失;且僅發生在日界前後的窄窗與剛好切齊的區間。去重正確性不受影響(`mergeCompletionRecords` 只看 `clientRecordId`,不看日期)。可在後端補彙總端點那一輪一併處理 |
| O-2 | **`filterByDateRange` 的單邊區間語意**:規格 E-02 說「`from`/`to` 為空字串時本機列不做該端過濾」,但既有 `reportUtils.js:39-40` 的實作是「任一端缺就整個不過濾」 | 只給 `from` 不給 `to` 時,本機列完全不過濾 | 這是 S3 之前就存在的既有行為,非本輪引入;且兩個入口(`ReportsPage`、`useAnalysisData`)實際上永遠同時提供 `from` 與 `to`,現行程式碼走不到這條路徑 |
| O-3 | **10,000 列的渲染成本**:決策 1 的硬上限是 `20 × 500`,取回上限比改接前的本機快取上限(1000 筆)高一個數量級,而明細表未做虛擬捲動 | 使用者選很寬的區間時可能渲染上萬列 | 規格已用 `truncated` 警示明確引導「請縮小日期區間」,不是靜默;且四個分頁的預設區間分別是今日 / 今日 / 當月 / 近 7 天,實務資料量遠低於上限。屬效能最佳化,不屬本輪「換資料來源」的驗收範圍 |

### T3-3 v1 提出的順序差異觀察 —— 已確認為改善而非偏差

規格 F3 步驟 2 寫「先讀本機快取並以 `filterByDateRange` 過濾,再 `normalizeLocalRecord`」,實作 `useProductionRecords.js:40-41` 是**先正規化再過濾**,並在 `:32-33` 註解說明理由:`normalizeLocalRecord` 會以 `finishedAt` 補 `date`,若先過濾,只有 `finishedAt` 而無 `date` 的紀錄會被誤殺。

本輪確認:實作順序的結果集是規格順序的**正確超集**,方向與規格原則 2「不讓使用者以為資料遺失」一致,且 AC-S4-20 已釘住區間邊界(區間外的本機列確實不混入)。判定為改善,不列為偏差;建議下一輪順手把此順序回寫規格 F3 步驟 2,避免規格與實作長期不一致。

> **T3 判定:PASS**

---

## 驗收結論

| 層 | 判定 | 依據 |
|---|---|---|
| T1 deterministic | **PASS** | 263 通過 / 1 略過,唯一失敗為規格豁免的 puppeteer e2e;lint 51/18 與基準線完全相同;DoD 4 grep 無結果;相依套件與後端零變更 |
| T2 semantic | **PASS** | v1 兩項缺失皆已補齊並實測驗證;F1~F6 與 §5、§9 逐項讀碼複查對得上規格;AC-S4-01~32 全數有測試且抽查為真檢查 |
| T3 judgment | **PASS** | 驗收標準可證偽、無矛盾、DoD 客觀;規格 §7 的 18 條 edge case 覆蓋充分;另找到三處未涵蓋情形,評估後均不阻擋,已列入下輪參考 |

**最終結論:PASS**(failedLayer=NONE、route=NONE)。

### 留給下一輪的非阻擋事項

1. **O-1 工廠日落差的標示措辭** —— 建議與 §8 backlog 第 1 項(後端彙總端點)同一輪處理。
2. **O-3 明細表虛擬捲動** —— 待現場實際資料量出來後再評估,現階段以 `truncated` 警示引導已足夠。
3. **規格 F3 步驟 2 的順序** —— 把「先正規化再過濾」回寫規格,消除規格與實作的文字落差。
4. **規格 E-02 與 `filterByDateRange` 單邊區間語意的落差(O-2)** —— 若日後有入口只提供單邊區間,需先修既有 `reportUtils` 的實作。
