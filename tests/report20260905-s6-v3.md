# report20260905-s6-v3 — S6 v3 驗收報告(Tester)

- **對應規格**:`docs/spec20260905-s6-v3.md`
- **分支**:`fix/audit-20260903`(工作區未 commit)
- **驗收時間**:2026-09-05 21:11 ~ 21:13
- **結論**:**FAIL**,卡在 **T2 semantic**,退回 **RD**
- **一句話**:程式碼與測試(D-1 / D-2)完全正確、全部指令綠燈,但 **D-4(`docs/report20260905-2.md` 的 §13 第 8 ~ 10 項)一個字都沒動**,規格 §11 列的四個交付檔只完成兩個,DoD §12 第 7 項不成立。

---

## T1 · deterministic —— **PASS**

全部指令由 Tester 親自執行,不採信 RD 說法。前置已執行 `find . -name "._*" -delete`。

| # | 指令 | 實測結果 | 規格門檻 | 判定 |
|---|---|---|---|---|
| T1-1 | `dotnet test backend/PrintingIoT.sln`(`dangerouslyDisableSandbox: true`) | `已通過! - 失敗: 0,通過: 249,略過: 0,總計: 249,持續時間: 4 s` | AC-62:全綠且維持 **249** | ✅ |
| T1-2 | `cd frontend && npm run test` | `Test Files 1 failed \| 32 passed (33)` / `Tests 297 passed \| 1 skipped (298)`;唯一失敗為 `tests/dashboard.e2e.test.js`(puppeteer:`The browser is already running for /tmp/claude-501/puppeteer_dev_chrome_profile-…`) | AC-63:相對 295 過 / 1 略過**零新增失敗**、總數 **+2** | ✅ 297 = 295 + 2,失敗檔仍是規格基準線明列「不修、不計、不得當回歸」的那一檔 |
| T1-3 | `npx vitest run src/tests/components/GeneralTabRoster.test.jsx` | `Test Files 1 passed (1)` / `Tests 2 passed (2)` | AC-66 / AC-67 有對應測試且通過 | ✅ |
| T1-4 | `npx vitest run src/tests/security src/tests/modules/LoginRoster.test.jsx` | `Test Files 3 passed (3)` / `Tests 10 passed (10)` | AC-59 ~ AC-61、AC-68 不得因本輪改動而紅 | ✅ |
| T1-5 | `cd frontend && npm run lint` | `✖ 67 problems (49 errors, 18 warnings)` | AC-64:相對基準線 **49 / 18** 零新增 | ✅ 逐數字相同 |
| T1-6 | `cd frontend && npm run build` | `✓ built in 1.91s`,僅 chunk >500kB 的既有警告 | AC-65:EXIT=0 | ✅ |

T1 結論:**PASS**,可往下檢查 T2。

---

## T2 · semantic —— **FAIL**(route = RD)

### T2-A · D-1 程式碼修正(§7.2)—— 通過

| 規格要求 | 實測(檔案:行號) | 判定 |
|---|---|---|
| `GeneralTab.jsx` 名冊白名單重建的遞補鏈改為 `u?.username ?? u?.id ?? u?.name ?? ''`(原 `:63`) | `frontend/src/components/settings/GeneralTab.jsx:66` `username: u?.username ?? u?.id ?? u?.name ?? '',` | ✅ |
| `handleAddUser` 建立列物件改為 `username: newUserCode`(原 `:111`) | 同檔 `:117` `username: newUserCode,` | ✅ |
| §7.5 兩個元件遞補鏈「逐字一致」 | `GeneralTab.jsx:66` 與 `frontend/src/modules/auth/LoginModal.jsx:18` 皆為 `username: u?.username ?? u?.id ?? u?.name ?? '',`,逐字相同(E24 成立) | ✅ |
| §1.2 只碰那兩處,其餘區塊一行不動 | `git diff` 顯示本檔僅該兩處運算式改變;其餘 hunk(移除 `newUserPassword` / 密碼欄 / 密碼表頭)屬 S5 既有未 commit 變更,`docs/spec20260905-s6-v3.md:29-30` 已記載 v1 當時該檔即為此狀態 | ✅ |
| §7.1 `LoginModal.jsx` 不得回退 | `LoginModal.jsx:18`(`sanitizeRoster`)、`:162`(`username: newUserCode`)、`:113`(`setLoginUsername(u.username || u.name || '')`)皆維持 v1 狀態 | ✅ |
| 不新增 i18n 鍵、不新增相依套件 | diff 內無 i18n 鍵新增、`package.json` 未動;`localeParity` 相關測試全綠(T1-2) | ✅ |
| E25:編輯改代碼後 `username` 跟著代碼走 | `GeneralTab.jsx:133-138` 以 `newUsers[existingIdx] = newUser` **整列取代**,`newUser.username = newUserCode`,不會殘留舊代碼 | ✅ |
| 註解用正體中文、函式級 | `:63-65`、`:100-107` 為正體中文函式級註解,說明輸入 / 輸出 / 邏輯 | ✅ |

### T2-B · D-2 新增測試(§9.3)—— 通過

`frontend/src/tests/components/GeneralTabRoster.test.jsx`(新檔,89 行):

- 渲染**真實** `GeneralTab` 元件並外包 `LanguageProvider`,未對純函式打樁,斷言對象是實際寫進 `localStorage.appUsers` 的形狀 —— 符合 §9.3 實作提示。
- `vi.mock('../../services/api')` 提供 `getReasonCodes` / `createReasonCode` / `deleteReasonCode` 三個匯出;`window.alert` / `window.confirm` 以 spy 取代。
- 兩條測試各自 `beforeEach` / `afterEach` `localStorage.clear()`,無互相污染。
- 取欄位用的字面值與 i18n 定義一致:`frontend/src/modules/language/LanguageContext.jsx:41` 中 `settingsExt.general.userNamePlaceholder = '使用者'`、`idPlaceholder = '代碼'`,測試以 `getByPlaceholderText('使用者' / '代碼')` 取得,語意正確。
- **AC-66**:斷言 `row.username === 'OP1'`、`!== '王小明'`、`row.name === '王小明'`,並附帶 `'password' in row === false`(AC-61 / AC-68 順帶保護)。✅
- **AC-67**:預先寫入 `[{ id: 'OP2', name: '李小華' }]`,掛載後斷言 `row.username === 'OP2'`、`!== '李小華'`。✅

### T2-C · D-4 交付報告更新(§13 第 8 ~ 10 項)—— **不通過,這是本輪 FAIL 的唯一原因**

`docs/report20260905-2.md` 共 388 行,**與 v1 交付時完全相同,沒有任何 v3 增量**。逐項核對:

| 規格條目 | 要求 | 實測證據 | 判定 |
|---|---|---|---|
| §13 第 **8** 項 | 改寫第 (6) 項「前端名冊 `username` 修正」:寫明**兩個寫入點**、E20 三條修正途徑、明說「本版之前在設定頁重新儲存會再次寫入顯示名稱」、補 AC-68 | `docs/report20260905-2.md:149-158` 仍只寫 `frontend/src/modules/auth/LoginModal.jsx` 一個檔;`:155` 仍是原句「管理者把該列**重新儲存一次**(Upsert 覆寫)即修正(E20)」——**這正是 spec v3 §0 判定為錯誤、要求改寫的那一句**;`:158` 仍只引 AC-61。全檔 `grep -n "寫入點\|AC-66\|AC-67\|AC-68"` **零命中** | ❌ |
| §13 第 **9** 項 | 在**重複帳號檢查 SQL 旁**加註 `UPPER(TRIM())` 與 C# `Trim().ToUpperInvariant()` **只在 ASCII 帳號上等價** | 檢查 SQL 位於 `:184-187`,其後 `:188-193` 四點說明**完全沒有** ASCII 等價性註記。檔內唯一提到 ASCII 的是 `:171-172`,位於第 (7) 節「明確未納入本輪」,講的是「中文帳號 `ToUpperInvariant` 無作用」,**與 SQL 對 C# 的等價性是不同命題**,不能代替 | ❌ |
| §13 第 **10** 項 | 把「§4.5 第 5 步」/「§4.5」改為「本報告 §2 第 5 步」/「本報告 §2」,**至少涵蓋 `:35` 與 `:306` R2 兩處** | `:35` 仍為「上線順序必須嚴格照 **§4.5**(見下方「部署者操作順序」)」;`:306` R2 仍為「完成 **§4.5** 第 5 步之前…必須嚴格照 **§4.5**」。全檔 `grep -n "本報告 §"` **零命中** | ❌ |
| §7.1 變更檔案清單 | 本輪動到的檔案應反映在報告 | `:384` 前端(修改)仍只列 `src/modules/auth/LoginModal.jsx`,未列 `src/components/settings/GeneralTab.jsx`;`:386` 前端(新增測試)仍只列 `LoginRoster.test.jsx`,未列 `GeneralTabRoster.test.jsx` | ❌(併入第 8 項一起補) |

規格 §11「本版(v3)實際要動的檔案 —— 只有這四個」列了四個,RD 只完成兩個(`GeneralTab.jsx`、`GeneralTabRoster.test.jsx`);`docs/report20260905-2.md` 未動。DoD §12 第 7 項「交付報告已依 §13(含新增的第 8 ~ 10 項)更新完成」因此不成立。

**為何判 RD 而非 PM**:規格 §13 把三項更正的**落點行號、要改的句子、允許更動的 token 範圍**都寫死了(第 10 項甚至限定「只准更換章節編號這個 token,其餘一字不得更動」),不存在歧義或矛盾,純粹是漏做。

### T2 結論:**FAIL**,依驗收流程停止,**不往下檢查 T3**。

---

## T3 · judgment —— 未執行

T2 未通過,依規則停止。下一輪 T2 通過後才進行 T3。

---

## 下一輪 RD 的最小工作項(照做即可過)

**只改 `docs/report20260905-2.md` 一個檔,程式碼與測試一行都不要動**(D-1 / D-2 已驗過,回退會讓 AC-66 / AC-67 變紅)。

1. **改寫 `:149-158` 第 (6) 節**(spec §13 第 8 項):
   - 明寫名冊 `username` 的寫入點**有兩個** —— `frontend/src/modules/auth/LoginModal.jsx`(登入視窗管理者子面板)與 `frontend/src/components/settings/GeneralTab.jsx`(設定 → 一般 → 使用者名冊),兩者寫同一份 `localStorage.appUsers`,**本版兩處都已修正**。
   - 舊資料修正途徑照 spec §10 E20 寫三條(① 手動輸入正確帳號 ② 設定頁重新儲存 ③ 登入視窗管理者子面板重新儲存),並加上「**本版之前在設定頁重新儲存會再次寫入顯示名稱,必須升級到本版後再操作**」。
   - 刪除 / 取代 `:155` 現有那句「管理者把該列重新儲存一次(Upsert 覆寫)即修正」。
   - 補「名冊仍然不含 `password` 欄位(AC-61 / AC-68 回歸釘住)」。
2. **在 `:187` 的 SQL 區塊之後補一點**(spec §13 第 9 項):`UPPER(TRIM())` 與 C# `Trim().ToUpperInvariant()` **只在 ASCII 帳號上等價**(非 ASCII 字母的大小寫映射、以及 `TRIM` 與 `String.Trim()` 對部分 Unicode 空白字元的處理可能不同);現場帳號一律 ASCII 代碼,故不影響本次部署(E21)。**不得寫成通用保證。**
3. **只換章節編號 token**(spec §13 第 10 項):`:35` 的「§4.5」→「本報告 §2」;`:306` R2 的「§4.5 第 5 步」→「本報告 §2 第 5 步」、同句後段「§4.5」→「本報告 §2」。句子其餘字句、語氣與強度**一字不得更動**;R1 ~ R10 其餘條目不動。(`:176` 標題「(規格 §4.5,原樣照抄)」指的是 S5 規格出處,不在 §13 明列範圍,可保留。)
4. **更新 `:384` / `:386` 的 S6 變更檔案清單**:前端(修改)加 `src/components/settings/GeneralTab.jsx`;前端(新增測試)加 `src/tests/components/GeneralTabRoster.test.jsx`(AC-66 / AC-67)。
5. 不得軟化 spec §13 明列必須原樣保留的內容:密碼 `eric4577599` 必須全面更換、「完成部署第 5 步之前,現場完全無法操作系統」、部署七步順序 / 上線檢查清單 / R1 ~ R10。

改完後重跑 T1 六項確認零回歸(純文件改動理論上不影響,但仍須實測)。
