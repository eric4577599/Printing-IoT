# tests/report20260905-s6-v1 — S6 驗收報告(Tester)

- **對應規格**:`docs/spec20260905-s6-v1.md`
- **驗收時間**:2026-09-05 20:46 ~ 20:52
- **驗收人**:Tester subagent(獨立執行,不採信 RD 的說法)
- **結論**:**FAIL**,卡在 **T2 semantic**,退回 **RD**
- **一句話**:程式碼、測試、migration 全部到位且實測全綠,但規格 §1.1-6 / §11 / §12-7 / §13 明訂必須更新的交付報告 `docs/report20260905-2.md` 完全沒動,部署者拿到的仍是 S5 版本、且內含已過時的錯誤敘述。

---

## T1 · deterministic —— **PASS**

四項全部由本 Tester 親自執行,以下為實際輸出。

### T1-1 後端 `dotnet test`(AC-62)

```
$ find . -name "._*" -delete
$ dotnet test backend/PrintingIoT.sln
  PrintingIoT.Core -> .../PrintingIoT.Core.dll
  PrintingIoT.Infrastructure -> .../PrintingIoT.Infrastructure.dll
  PrintingIoT.Worker -> .../PrintingIoT.Worker.dll
  PrintingIoT.API -> .../PrintingIoT.API.dll
  PrintingIoT.Tests -> .../PrintingIoT.Tests.dll
已通過! - 失敗: 0，通過: 249，略過: 0，總計: 249，持續時間: 4 s
```

- 基準線 237 → **249**,增加 12 條,**只增不減**,符合 AC-62。
- 12 條的來源可逐條對上:AC-47 ~ AC-56 十條(`AuthSetupAndUsersTests.cs`)+ AC-57 / AC-58 兩條(`RateLimitAndCorsTests.cs`)。無多餘、無短少。
- 建置零錯誤零警告,migration 與 Designer 皆能編譯(規格 §12-5 的「能編譯」成立)。

### T1-2 既有測試是否被改動(AC-62 後半)

```
$ git diff backend/PrintingIoT.Tests/ | grep -n "UsernameNormalized\|AppUsernames"
(無輸出)
```

- 工作區內被修改的既有測試檔(`AuthControllerTests.cs`、`ErpControllerTests.cs`、`OpenApiContractTests.cs`、`OrdersSyncControllerTests.cs`、`ProductionCompletionsControllerTests.cs`、`QA_Scenarios_Tests.cs`、`ReasonsControllerTests.cs`)的 diff **完全不含** S6 的任何識別字,可判定為 S5 遺留的未提交改動,非本輪所為。
- 亦即 §3.3 的「實體層自動同步」確實達成了它的目的:既有 `new User { Username = "op1", ... }` 一律不需修改。**AC-62 成立**。

### T1-3 前端 `npm run test`(AC-63)

```
$ cd frontend && npm run test
 Test Files  1 failed | 31 passed (32)
      Tests  295 passed | 1 skipped (296)
```

- 唯一失敗檔為 `frontend/tests/dashboard.e2e.test.js`(puppeteer,錯誤為 `The browser is already running for /tmp/.../puppeteer_dev_chrome_profile-...`),屬規格基準線明列的「不修、不計、不得當回歸」項目。
- 通過數 292 → **295**,增加 3 條,正是 AC-59 / AC-60 / AC-61。**零新增失敗,AC-63 成立**。

### T1-4 前端 `npm run lint`(AC-64)與 `npm run build`(AC-65)

```
$ npm run lint
✖ 67 problems (49 errors, 18 warnings)
```

- 與基準線 **49 errors / 18 warnings 完全一致**,零新增。**AC-64 成立**。
- 抽查列出的錯誤(`orderMapper.js:42`、`productionStore.test.js:6`、`dashboard.e2e.test.js:36` 等)皆為本輪未觸及的檔案。

```
$ npm run build
✓ built in 1.94s
```

- 建置成功。**AC-65 成立**。

**T1 判定:PASS。**

---

## T2 · semantic —— **FAIL**

### T2-A 後端實作與規格逐條比對 —— 全部相符

| 規格條目 | 實作位置 | 核對結果 |
|---|---|---|
| §2.2 正規化定義 `Trim().ToUpperInvariant()` | `backend/PrintingIoT.Core/Constants/AppUsernames.cs:19` | 完全一致,null 回空字串;註解正體中文、函式級、寫明輸入 / 輸出 / 邏輯,並註明與 `AppRoles.Normalize` 同慣例 |
| §3.3 實體層自動同步、`private set` | `backend/PrintingIoT.Core/Entities/Auth/User.cs:23-31`(setter 內呼叫 Normalize)、`:40`(`private set`) | 相符。`_username` 私有欄位 + setter 導出,任何寫入路徑都不可能遺漏 |
| §3.5 Login 改查 `UsernameNormalized` | `backend/PrintingIoT.API/Controllers/AuthController.cs:56`(C# 端先算 `normalized`)、`:61`(`u.UsernameNormalized == normalized`) | 相符,且**未**寫成 `u.Username.ToUpper()`,§3.5 末段的索引失效陷阱已避開 |
| §3.5 其餘登入邏輯不動 | 同檔 `:63`(`IsActive`)、`:69`(BCrypt)、`:65/71`(訊息不區分原因)、`:52`(`[EnableRateLimiting("auth")]`) | 相符,S5 行為原樣保留 |
| §3.5 CreateUser 改為 `AnyAsync` | 同檔 `:216-218` | 相符,記憶體全表比對已移除 |
| §3.5 兩處 `SaveChangesAsync` 包 `DbUpdateException` → 409 | 同檔 `:141-150`(SetupAdmin)、`:236-245`(CreateUser) | 相符,且註解誠實標註 InMemory 無法驗證此分支 |
| §3.5 UpdateUser 不動 | 同檔 `:267` 一帶 | 相符,無 username 相關改動 |
| §3.5 保留 `Username` 唯一索引、新增 `IX_Users_UsernameNormalized` | `backend/PrintingIoT.Infrastructure/Data/PrintingContext.cs:73-75`、`:80-83` | 相符,兩個索引並存 |
| §4 migration 四步順序 | `.../Migrations/20260905010000_AddUsernameNormalizedUniqueIndex.cs`:AddColumn(nullable) → `UPDATE ... UPPER(TRIM(...))` → AlterColumn(NOT NULL) → CreateIndex(unique) | 順序完全一致;Down 為 DropIndex → DropColumn;檔頭註解已寫入部署前置檢查 SQL 與「不做自動合併」 |
| §4 Designer + ModelSnapshot | `20260905010000_...Designer.cs:81/91/93`、`PrintingContextModelSnapshot.cs:78/88/90` | 兩者皆已更新 |
| §8.2 `UsernameNormalized` 不外洩 | AC-55 測試斷言 + `UserSummary` 未含該欄 | 相符 |

### T2-B 測試是否真的驗到規格所述的行為 —— 全部相符

逐條抽查 AC-47 ~ AC-61 的斷言內容(非只看測試名稱):

- **AC-47**(`AuthSetupAndUsersTests.cs:309-327`):建 `OP1` → 201,以 `op1` 登入 → 200,且 `body.Username == "OP1"`。核心 AC 確實驗到「原始大小寫回傳」,不是只驗登入成功。
- **AC-48**(`:330-345`):`setup-admin` 建 `Admin1`,以 `admin1` 登入 → 200 且回傳 `Admin1`。
- **AC-49**(`:348-359`):`" op1 "` 前後空白 → 200。
- **AC-50**(`:361-372`):`StrongPassword.ToUpperInvariant()` → 401。**密碼精確比對的反向釘樁確實存在**,規格 §1.2 的紅線有守。
- **AC-51**(`:374-387`):空 Users 表 → 401,並另開 scope 斷言 `Assert.Empty(context.Users)`,兩件事都驗。
- **AC-52**(`:389-408`):繞過 API 直接 `context.Users.Add(new User { Username = "Op9", ... })`,**另開一個 scope 讀回**再斷言 `UsernameNormalized == "OP9"` —— 沒有靠同一個 change tracker 取巧。
- **AC-53**(`:410-427`):以 `context.Model.FindEntityType(typeof(User))!.GetIndexes()` 同時斷言兩個 `IsUnique` 單欄索引存在,與規格寫法一致。
- **AC-54**(`:430-449`):停用後以 `OP1` 登入仍 401,正規化未旁路 `IsActive`。
- **AC-55**(`:451-469`):斷言 raw JSON 含 `"OP1"` 且 `DoesNotContain("usernameNormalized", OrdinalIgnoreCase)`,不分大小寫比對,與規格要求一致。
- **AC-56**(`:472-489`):`OP1` → 201、`op1` → 409。
- **AC-57**(`RateLimitAndCorsTests.cs:114-127`):`OPTIONS /api/orders` + `Origin: http://localhost:5600` + `Access-Control-Request-Method: POST`、**刻意不帶權杖**;三重斷言(非 401、2xx、`Access-Control-Allow-Origin` 等於該來源)。與 §5.2 表格逐項對應。
- **AC-58**(`:129-140`):`http://evil.example.com` → 不得帶 `Access-Control-Allow-Origin`。反向釘樁到位,`AllowAnyOrigin` 的退化路徑被封住。
- **AC-59 ~ AC-61**(`frontend/src/tests/modules/LoginRoster.test.jsx:59/74/87`):走真實 `LoginModal` 元件、檢查真正寫進 `localStorage.appUsers` 的資料形狀,不是對純函式打樁;AC-61 另外驗了舊資料的 `password` 會被剝除且 `'1234'` 不殘留於序列化字串。

前端實作對應:`frontend/src/modules/auth/LoginModal.jsx:18`(`u?.username ?? u?.id ?? u?.name ?? ''`)與 `:162`(`username: newUserCode`),與規格 §7 表格兩列完全一致;未新增 i18n 鍵(`localeParity` 測試仍綠)。

### T2-C 紅線複查 —— 全部有守

- 無新增 NuGet / npm 相依:`git status` 中無任何 `*.csproj`、`package.json`、`package-lock.json` 異動。
- 無 `docker-compose.yml` / mosquitto / Cloudflare Tunnel 改動。
- 未擴大範圍:`UpdateUser` 未動,ERP M2M 未碰。

### T2-D **未達成項目 —— 這是 FAIL 的原因**

**交付報告 `docs/report20260905-2.md` 完全沒有更新。**

證據一,檔案時間戳落在 S6 開工之前:

```
$ ls -l docs/report20260905-2.md backend/PrintingIoT.Core/Constants/AppUsernames.cs
-rw-r--r--  ... 1217   9月  5 20:39  backend/PrintingIoT.Core/Constants/AppUsernames.cs
-rw-r--r--  ... 14540  9月  5 17:08  docs/report20260905-2.md
```

S6 的程式碼寫於 20:39,報告最後修改停在 17:08(S5 時間),中間未再被寫入。

證據二,§13 要求「必須新增」的七項,實測**全數缺漏**:

```
$ grep -n "不分大小寫\|20260905010000\|重複帳號\|AC-57\|AC-58\|無 body" docs/report20260905-2.md
(無任何一項命中)
```

| §13 必須新增項 | 現況 |
|---|---|
| 1. 登入帳號自本版起不分大小寫、密碼仍精確比對 | **缺** |
| 2. 部署第 3 步前先跑重複帳號檢查 SQL | **缺**(此項缺漏會讓部署者直接踩 E18:migration 建唯一索引失敗回滾) |
| 3. 新增 migration `20260905010000_...`、本機無法套用 | **缺**(部署者不知道有第二個 migration 要套) |
| 4. CORS 預檢已補 AC-57 / AC-58,且誠實說明先前並非已證實缺陷 | **缺** |
| 5. `setup-admin` 無 body 回 400 而非 404 的文件更正 | **缺** |
| 6. 前端名冊 `username` 修正與 E20 的重新儲存修法 | **缺** |
| 7. 明確標註未納入本輪:ERP M2M、`RequireAuth.jsx` 403 文案 i18n | **缺** |

證據三,**報告現存內容已因本輪改動而變成錯誤敘述**,不是單純的「少寫」:

- `docs/report20260905-2.md:177`
  「CORS 預檢實測(E6)|…**本輪也未加測試**。順序已依規格修正,**實際 `OPTIONS` 預檢行為尚未經自動化驗證**,建議 S6 補一條 AC」
  → AC-57 / AC-58 已存在並通過,這句話現在是假的。
- `:174`「前端使用者管理 UI 接後端 …S6 接 `/api/v1/auth/users`」與 `:169` 的「留給 S6 的事」整節,在 S6 已經做完一部分之後未重新校準,會讓部署者誤判目前進度。

規格對此的要求密度極高,不存在解讀空間:§1.1 第 6 項列為在範圍內、§11「文件(修改,不另開新檔)」、§12 DoD 第 7 項、§13 整節七條。**這是應交付而未交付的工作項,非我方加碼要求。**

**T2 判定:FAIL → route RD。**

---

## T3 · judgment —— 未執行

依驗收流程,T2 未過即停止,不往下檢查。

先行記錄一則供 RD 補件時一併處理的觀察(**不構成本次退回理由**):`docs/report20260905-2.md:169` 的「已知限制與留給 S6 的事」整節,在 S6 完成後應改寫為「留給 S7」,否則交付文件會自相矛盾。

---

## 退回 RD 的具體修正指示

**只需補一件事,程式碼一行都不必動**(T1 全綠、T2 程式面全部相符,不要順手重構)。

依 `docs/spec20260905-s6-v1.md` §13 更新既有的 `docs/report20260905-2.md`(**不另開新檔**):

1. 補齊 §13「必須新增」七項,逐條落地,尤其第 2 項的部署前置檢查 SQL 必須**寫在 §4.5 部署步驟第 3 步之前的位置**,不能只放在文末附註 —— 部署者是照步驟往下做的。
2. 修正 `:177` 那一列的 CORS 敘述:改為「AC-57 / AC-58 已補,`OPTIONS` 預檢行為已有自動化回歸保護」,並依 §13-4 誠實說明先前並非已證實的缺陷,而是契約與回歸保護的缺口。
3. `:169` 的「留給 S6 的事」整節改寫為「留給 S7」,並把已於 S6 完成的項目移出。
4. §13「必須原樣保留、用字不可軟化」的三處(密碼 `eric4577599` 必須全面更換 / 第 5 步前現場完全無法操作 / §4.5 七步與 R1~R10)**逐字保留**,補寫時不得順手改寫語氣 —— 現況這三處都還在(`:22-23`、`:28`、`:83`、`:98`、`:156-158`),不要動到。

補完後回測只需確認 `docs/report20260905-2.md` 的內容,T1 的四道指令無需重跑(本輪已全綠且不會受文件改動影響)。
