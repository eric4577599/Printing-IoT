# report20260906-4 — 上線執行紀錄(G70Pro 這台機器的堆疊)

- **輪次**:實際執行 `docs/report20260906-2.md` §2 的 A–E 五段
- **日期**:2026-09-06 21:12–21:30
- **對象**:本機 docker compose 堆疊(`printingiot-*`,經 Cloudflare Tunnel 對外)
- **狀態**:**已完成**。系統已上鎖、帳號已建立、可正常登入。

---

## 0. 先更正一件事:Docker 在這台機器是可用的

`docs/report20260905-2.md`(R5)、`docs/report20260906-1.md`、`docs/report20260906-2.md`
與 `handoff.md` 都寫著「Docker 在這台機器不可用(unix socket 權限被拒)」。

**那是誤判。** 真正的原因是 **Claude Code 的 Bash 沙箱擋掉了 docker 的 unix socket**;
帶 `dangerouslyDisableSandbox: true` 執行時,`docker info` 回報 **daemon 29.4.1**,一切正常。

影響:先前四份報告裡「本機無法套用 migration、無法連真實資料庫、runbook 無法在測試環境走過」
這些自我限制**全部是不成立的**。本輪就是在這台機器上把整段跑完的。

教訓已寫進 L2 → `[[feedback-sandbox-false-negative]]`

---

## 1. 實際執行結果

### A 段 · 前置

| 步驟 | 結果 |
|---|---|
| 備份 | `backup-FlexoDB-20260906-2113.sql`(11K),已加進 `.gitignore` |
| `./scripts/preflight-check.sh` | **exit 0** |

前置檢查對**真實 PostgreSQL** 跑出來的結論:

- 已套用 **5** 個 migration、待套用 **8** 個 —— 與 `report20260906-2.md` §1.2 的靜態分析完全一致
- **無僅大小寫不同的重複帳號**、**無重複 ProductCode** → 兩個唯一索引可安全套用
- `Users` 表是空的 → 必須走 setup-admin

**這同時補上了 `report20260906-2.md` §6.3 標註的缺口**:該腳本原本只用假 `docker` 驗過邏輯,
SQL 字面正確性未經真實 psql 執行。本次實跑證明 SQL 全部正確,無語法或欄位名錯誤。

資料庫實際內容(精確計數,不是 `n_live_tup` 估計值):

| 資料表 | 列數 |
|---|---|
| MachineSections | 8 |
| Orders | 2 |
| Products / ProductionLogs / Users / Roles | 0 |

### B 段 · 拉起新版

`docker compose up -d --build` —— 三個映像重建,API 啟動時自動套用 8 個 migration:

```
Applying migration '20260903000000_AddProductCodeUniqueIndex'.
… (8 個)
PrintingContext migrated successfully.
Now listening on: http://[::]:8080
```

複查:**13 個全部套用、0 待套用**。`GET /api/orders` 未帶權杖回 **401**(認證生效),
`POST setup-admin` 回 **404**(token 為空),前端 :5600 回 **200**。

### C 段 · 建立帳號

`AUTH_SETUP_TOKEN` 以 `openssl rand -hex 32` 產生寫入 `.env` → 重啟 API →
建立 `admin`(ADMIN)→ 以其權杖建立 `OP1`~`OP5`(OPERATOR,班別 A)。

密碼由 `secrets.choice` 產生,16 字元、保證含字母與數字,存於 **`.credentials-20260906.txt`**
(已加進 `.gitignore`,**明文,請盡快移到密碼管理器並刪除該檔**)。

### D 段 · 收尾

`AUTH_SETUP_TOKEN` 清空 → 重啟 API → `setup-admin` 回 **404** ✅、未帶權杖回 **401** ✅。

### E 段 · 驗收(14 項全過)

| 檢查 | 實際 | 預期 |
|---|---|---|
| `OP1` / `op1` / `Op1` / `" op1 "` 皆可登入,回傳 username 一律 `OP1` | 200 ×4 | 200 |
| 密碼大小寫不同必須失敗 | 401 | 401 |
| 登入回傳刷新憑證(S7 G4) | 有 | 有 |
| 停用 OP5 → 立刻登不進去 → 重新啟用 → 又可登入(S7 G2) | 204 / 401 / 200 | 同 |
| `GET /api/production/summary/{daily,monthly,stop-reasons}`(S8) | 200 ×3 | 200 |
| 彙總端點日期格式錯 → 400、未帶權杖 → 401 | 400 / 401 | 同 |

**S8 的三支彙總端點自此已對真實 PostgreSQL 驗證過**,補上 `report20260906-3.md` §7 第 2 點的缺口
(`Shift.ToUpper()` 在 Npgsql 的翻譯正常,查詢無例外)。

---

## 2. 還沒做的

| 項目 | 說明 |
|---|---|
| **瀏覽器端 E1 / E5 / E6** | 完工→重載不復活、`/docs` 需登入、`/debug` 被擋 —— 需真人開瀏覽器走一次 |
| **E3 放超過 2 小時** | 權杖自動換發要等時間到,無法即刻驗 |
| **`JWT_SECRET` 是否已輪替** | `.env` 裡有一組 64 字元的值,但**不確定是不是這次上線才產生的新值**。舊密鑰在版控歷史中,若這組是舊的,必須換掉後重啟 API(換掉會讓現有登入階段全部失效,要挑時間) |
| **密碼 `eric4577599` 在其他系統的更換** | 與本次部署無關但仍是最高優先的人工動作(R3) |
| **現場帳號的實際配置** | 目前 OP1~OP5 全部掛班別 A,實際幾個人、分哪些班要由現場確認後調整 |
| **ERP 金鑰** | `ApiKeys` 仍是空表,等 ERP 真的要串接那天再建(`report20260906-1.md` §2.1) |
