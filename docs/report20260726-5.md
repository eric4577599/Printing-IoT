# 任務報告 — Settings 頁 i18n backlog 清除

> 日期:2026-07-26(`date` 取得)
> 撰寫:Claude(RD 手動分工)
> 版次:report20260726-5
> 承接:report20260726-4(全站 i18n 三回合)§6 backlog

---

## 1. 範圍

清除 Settings 頁 7 個 tab 的硬編碼中文(審計 A 盤點 198 筆),使切換語言時同步更換。

## 2. 做法(workflow 分工)

3 個平行 developer subagent retrofit(各自 wire 元件 + 回傳五語鍵),我集中併入 LanguageContext 並驗證:
- Agent 1:GeneralTab(43 處)
- Agent 2:FormulaTab + BoxTypeTab(51 處)
- Agent 3:CommunicationTab + MachineTab + ReportTab + UnitTab(59 處)

**命名:新鍵放獨立 `settingsExt` 命名空間**(零風險,不動既有 `settings.*` 已用鍵);共用按鈕走 `settingsExt.common.{add,edit,delete,save,cancel,update,import,sheets}`(整合端定義)。

## 3. 變更檔案

| 檔案 | 變更 |
|---|---|
| `components/settings/GeneralTab.jsx` | 加 useLanguage;43 處接 `t('settingsExt.general.*'/'common.*')` |
| `components/settings/FormulaTab.jsx` | 24 處接 `settingsExt.formula.*` |
| `components/settings/BoxTypeTab.jsx` | 加 useLanguage;27 處接 `settingsExt.boxType.*` |
| `components/settings/CommunicationTab.jsx` | 28 處接 `settingsExt.comm.*` |
| `components/settings/MachineTab.jsx` | 18 處接 `settingsExt.machine.*` |
| `components/settings/ReportTab.jsx` | 10 處接 `settingsExt.report.*` |
| `components/settings/UnitTab.jsx` | 補漏 4 處 `settingsExt.unit.*` |
| `modules/language/LanguageContext.jsx` | 五語新增 `settingsExt`(common/general/formula/boxType/comm/machine/report/unit) |

保留既有 `t('settings.*')` 鍵(SettingsPage 側欄/各 tab 標題/部分欄位)不動。

## 4. 排除(未動,依規則)

- 種子資料值:MachineTab `DEFAULT_SECTIONS` 部位名、GeneralTab 預設班別/停機/不良原因、ReportTab `exceptionFilters` 預設、BoxTypeTab `DEFAULT_BOX_TYPES` 與 S1~S5/Leading/Body/Tail 標籤預設 —— 皆使用者可編輯資料,非 UI 標籤。
- 註解、console、CSV/匯出內容、純英文/token(OEE/mm/%/IP/MQTT/Advantech…)。

## 5. 測試與建置

- **used-but-undefined 檢查**:7 tab 檔用了 **178 個 `settingsExt.*` 鍵,0 個未定義**。
- `localeParity`:五語鍵集合完全一致 + 無空值(含 settingsExt);i18n 13 passed。
- 全套 vitest **91 passed**;`vite build` ✓。

## 6. 部署與驗證

- 重建 `printingiot-frontend-1` 上線。
- 瀏覽器實測(切英文,2026-07-26):**PASS**。
  - Formula tab:Formula Settings / Core Efficiency / Shortage Force Input Threshold…Sheets / Continuous Production Definition / Targets / Prep Time Indicator / Light Rule 全英文。
  - Box Type tab(原需求圖那頁):Box Type Settings / Basic Info / Length Definition / Correction: mm (offset) / Width Definition / Add-Edit-Delete 全英文 —— 原 盒型設定/修正值/寬度定義/圖面欄位皆隨語言切換。
  - 無點路徑字串、無殘留中文標籤;殘留中文皆為種子資料值(部位名/預設原因)。

## 7. 全站 i18n 現況

至此 backlog 主體(Settings 198)清除。**剩餘**:modals params / MaintenancePage / ProductFormModal / AddScheduleModal(~110,audit-B)、DocsPortal(31,audit-C)—— 映射見 report20260726-4 §6 + scratchpad。

## 8. 註記

- `settingsExt` 為求零風險而與既有 `settings.*` 並存;未來可評估合併。
- 未 `git push`(全域紅線;待 Eric 指示)。
