# 任務報告 — UI 對抗性稽核第二輪(analysis/settings/shell/api-contract)

> 日期:2026-07-24
> 分支:`feat/extract-maintenance`
> Workflow Run ID:`wf_e544d9cf-a8e`(55 agents,0 error,~2.3M tokens,~10 分鐘)

## 1. 方法

接續前一輪 handoff 標記的 ~20 筆待驗證項(analysis/settings/shell/api-contract),因原始清單與 workflow script 在舊 session,改以**重新獵捕 + 對抗性驗證**取得可重現結果:

- **Hunt**:4 區並行獵手(general-purpose agent),各自 Read/Grep 實碼,回傳結構化缺陷(真實 file:line + repro + impact)
- **Verify**:每筆缺陷由 3 個獨立視角(重現性/正確性/影響)對抗性反駁,**預設懷疑**,**≥2 視角確認才存活**

## 2. 結果:候選 17 筆 → 確認 14 筆 / 反駁淘汰 3 筆

| # | 嚴重度 | 區塊 | 類別 | 位置 | 摘要 | 票 |
|---|---|---|---|---|---|---|
| 1 | critical | analysis | i18n-missing | `LanguageContext.jsx:222` | Analysis 區塊絕大多數 i18n 鍵在所有語系皆不存在,畫面直接顯示原始 key 字串 | 3✓/0✗ |
| 2 | high | analysis | logic-error | `AnalysisPage.jsx:137` | 時間刻度(分/時/週/月)聚合為 no-op,選任何刻度圖表都只按「日」分組 | 3✓/0✗ |
| 3 | high | analysis | state-bug | `AnalysisPage.jsx:22` | groupedData(分類分組結果)計算後從未被使用,整個「分類條件」側欄不影響任何輸出 | 3✓/0✗ |
| 4 | high | api-contract | api-mismatch | `MachineTab.jsx:187` | handleMoveSection 以 (id, 部分物件) 呼叫 updateMachineSection,但該 API 只吃單一 section 物件 → 送到 /machine-sections/undefined,部位排序永遠失敗 | 3✓/0✗ |
| 5 | high | api-contract | api-mismatch | `MachineTab.jsx:98` | handleAddSection 用 s.order(實際欄位為 displayOrder)算 maxOrder → NaN;且 payload 用 order/is_active 與後端 DisplayOrder/IsActive 不符,新部位排序永遠為 0 | 3✓/0✗ |
| 6 | high | settings | api-mismatch | `MachineTab.jsx:187` | MachineTab 部位排序 API 呼叫參數簽章錯誤,▲▼移動順序永遠寫入失敗 | 3✓/0✗ |
| 7 | high | settings | api-mismatch | `MachineTab.jsx:103` | MachineTab 新增部位送出欄位名 order/is_active 與後端 displayOrder/isActive 不符且 maxOrder 為 NaN | 3✓/0✗ |
| 8 | high | shell | broken-handler | `MainLayout.jsx:509` | 點擊底部 F5+/F6+(良品/不良)按鈕派發非法鍵 'F5+'/'F6+',Dashboard 永不匹配 → 按鈕無作用 | 3✓/0✗ |
| 9 | medium | api-contract | logic-error | `MachineTab.jsx:159` | handleMoveSection 以 a.order - b.order 排序(欄位不存在→NaN 不排序),但畫面清單依 displayOrder 排序 → 上/下移交換到錯誤的相鄰部位 | 3✓/0✗ |
| 10 | medium | settings | state-bug | `CommunicationTab.jsx:206` | CommunicationTab 監控更新頻率(monitor_interval)永不持久化 | 2✓/1✗ |
| 11 | medium | settings | api-mismatch | `CommunicationTab.jsx:258` | CommunicationTab 測試連線傳入的 host/port 參數被 api.testMqttConnection 全部丟棄 | 3✓/0✗ |
| 12 | medium | settings | i18n-missing | `LanguageContext.jsx:1031` | i18n 缺鍵:越南文/泰文缺 settings.unit/formula/report,切語系後單位/公式/報表分頁顯示原始 key | 3✓/0✗ |
| 13 | low | settings | logic-error | `GeneralTab.jsx:87` | GeneralTab 修改使用者時若變更代碼(ID)會新增重複使用者而非更新 | 3✓/0✗ |
| 14 | low | shell | logic-error | `MainLayout.jsx:373` | 速度滑桿左端標籤標為 '0',但滑桿最小值 -1 代表『停止』、0 在中央代表『標準』,標籤誤導 | 2✓/1✗ |

## 3. 確認缺陷明細

### [1] [CRITICAL] Analysis 區塊絕大多數 i18n 鍵在所有語系皆不存在,畫面直接顯示原始 key 字串
- **區塊/類別**:analysis · i18n-missing　**票數**:3✓/0✗
- **位置**:`frontend/src/modules/language/LanguageContext.jsx:222`
- **說明**:translations 物件裡每個語系(tw/cn/en/vn/th)的 analysis 節點只有 title / charts / metrics / filters 四個子鍵(見 LanguageContext.jsx:222-245 tw、553 cn、884 en)。但 Analysis 元件大量呼叫下列不存在的鍵,且多數呼叫點沒有 `||` 後備字串:
- AnalysisPage.jsx:32-43 availableCategoryFields → t('analysis.fields.customer'/'product'/'boxNo'/'boxType'/'orderNo'/'qty'/'operator'/'shift'/'stopReason'/'prepTime'/'date')
- AnalysisPage.jsx:46-55 availableDisplayFields → t('analysis.fields.qty'/'prepTime'/'runTime'/'stopTime'/'avgSpeed'/'stopCount'/'defectQty'/'oee')
- AnalysisPage.jsx:57-63 chartOptions → t('analysis.chartTypes.pie'/'doughnut'/'line'/'bar'/'radar')
- AnalysisPage.jsx:65-71 timeScaleOptions → t('analysis.timeScales.minute'/'hour'/'day'/'week'/'month')
- AnalysisTable.jsx:27-39 表頭 → t('analysis.fields.date'/'orderNo'/'customer'/'product'/'shift'/'operator'/'goodQty'/'defectQty'/'prepTime'/'runTime'/'stopTime'/'avgSpeed'/'oee')
- AnalysisSidebar.jsx:70 t('analysis.dateRange')、:78 t(`analysis.quickDate.${type}`)、:86 t('analysis.category.title')、:105 t('analysis.display.title')、:150 與 AnalysisTable.jsx:16 t('analysis.actions.clearFilters')
LanguageContext 的 t()(:1073-1081)在找不到鍵時回傳 `value || key`,因此上述位置全部渲染成字面 key,例如表頭顯示「analysis.fields.date」、下拉選項顯示「analysis.chartTypes.pie」。預設語系為 tw(:1071),連預設中文也壞。
- **重現**:開啟生產分析頁 → 側邊「分類條件/顯示欄位」下拉、圖形類型/時間刻度選項、快速日期按鈕、以及切到表格視圖後的所有表頭,全部顯示 'analysis.fields.xxx' / 'analysis.chartTypes.xxx' 之類的原始鍵字串而非中文。
- **影響**:整個分析頁幾乎所有標籤、下拉選項、表頭、按鈕文字都變成無意義的英文 key,使用者完全無法辨識欄位與選項,功能形同不可用。

### [2] [HIGH] 時間刻度(分/時/週/月)聚合為 no-op,選任何刻度圖表都只按「日」分組
- **區塊/類別**:analysis · logic-error　**票數**:3✓/0✗
- **位置**:`frontend/src/pages/analysis/AnalysisPage.jsx:137`
- **說明**:chartData 的 aggregate 內:`const key = timeScale === 'day' ? r.date : r.date; // 簡化處理`(AnalysisPage.jsx:137)。三元運算子兩邊都是 r.date,無論 timeScale 是 minute/hour/week/month 都用日期字串當 key。timeKeys 也是 `Array.from(new Set(productionHistory.map(r => r.date)))`(:144),同樣只依日期。側邊的時間刻度 radio(AnalysisSidebar.jsx:137-142,setTimeScale)因此對圖表毫無作用。
- **重現**:在分析頁選「時間刻度=月」或「週」,圖表 X 軸與資料分組完全不變,仍是逐日的資料點;切換 minute/hour/week/month 圖表輸出一模一樣。
- **影響**:時間刻度切換功能完全失效,使用者無法依週/月/時/分彙總檢視趨勢,是明確的功能缺陷。

### [3] [HIGH] groupedData(分類分組結果)計算後從未被使用,整個「分類條件」側欄不影響任何輸出
- **區塊/類別**:analysis · state-bug　**票數**:3✓/0✗
- **位置**:`frontend/src/pages/analysis/AnalysisPage.jsx:22`
- **說明**:useAnalysisData 依 selectedCategories 計算 groupedData(useAnalysisData.js:38-50),AnalysisPage 於第 22 行解構取得 groupedData,但整個 render(:177-216)完全沒有使用它:圖表資料 chartData(:125-158)直接對未分組的 productionHistory 聚合,AnalysisTable(:196)也直接吃 productionHistory。因此側邊「分類條件」區(AnalysisSidebar.jsx:84-101,handleAddCategory / handleCategoryChange / setSelectedCategories)新增、切換、移除分類後,圖表與表格毫無變化。此外 pie/doughnut/radar 這類分佈圖(isDistributionChart)理應呈現分類分佈,但 chartData 仍是逐日時間序列,等同分佈圖也沒有依分類繪製。
- **重現**:在分析頁「分類條件」新增『客戶』或『班別』並切換,圖表與表格輸出完全不變;切到圓餅圖,切片仍是逐日良品數而非各客戶/班別分佈。
- **影響**:分類/分群這一整組核心分析功能是死的,使用者以為在依維度分析,實際上永遠只看到全體未分組資料,產生誤導。

### [4] [HIGH] handleMoveSection 以 (id, 部分物件) 呼叫 updateMachineSection,但該 API 只吃單一 section 物件 → 送到 /machine-sections/undefined,部位排序永遠失敗
- **區塊/類別**:api-contract · api-mismatch　**票數**:3✓/0✗
- **位置**:`frontend/src/components/settings/MachineTab.jsx:187`
- **說明**:handleMoveSection 呼叫 `apiUpdateSection(currentSection.id, { displayOrder: newCurrentOrder })` 與 `apiUpdateSection(targetSection.id, { displayOrder: newTargetOrder })`,傳入兩個引數 (guid 字串, 部分 payload)。但 services/api.js 的 `updateMachineSection = async (section) => api.put(`/settings/machine-sections/${section.id}`, section)` 只接受單一物件並讀 `section.id`。此處第一個引數是 guid 字串,`"guid字串".id` 為 undefined,因此請求打到 `/api/settings/machine-sections/undefined`,body 也變成整串 guid 字串而非 MachineSection。後端 `UpdateMachineSection(Guid id, MachineSection section)` 無法把 "undefined" 綁成 Guid → 400。第二個引數(真正想更新的 displayOrder)完全被忽略。對照同檔 line 238 正確用法 `apiUpdateSection(section)` 傳整個物件即可證明此處簽章用錯。連帶即使 URL 正確,只送 `{displayOrder}` 也會讓後端把 [Required] 的 Name 等欄位清空而驗證失敗。
- **重現**:設定頁→機台設定,選一個部位,點▲或▼移動順序 → 後端回 400/404,前端 catch 跳出 alert「順序更新失敗 (Failed to update order)」,DB 的 displayOrder 從未更新,重新整理後順序復原。
- **影響**:機台部位「上移/下移」排序功能完全無法儲存,每次操作都彈錯誤視窗;部位顯示順序無法持久化。

### [5] [HIGH] handleAddSection 用 s.order(實際欄位為 displayOrder)算 maxOrder → NaN;且 payload 用 order/is_active 與後端 DisplayOrder/IsActive 不符,新部位排序永遠為 0
- **區塊/類別**:api-contract · api-mismatch　**票數**:3✓/0✗
- **位置**:`frontend/src/components/settings/MachineTab.jsx:98`
- **說明**:line 98 `Math.max(...machineSettings.sections.map(s => s.order))`,但 MachineSection 實體與整個檔案其他地方用的是 `displayOrder`(見 line 173-181、247、344,及 backend Entities/MachineSection.cs 的 `public int DisplayOrder`)。`s.order` 對每個 section 都是 undefined → `Math.max(undefined, ...)` = NaN → `maxOrder + 1` = NaN。接著 line 101-105 送出的 payload 為 `{ name, order: maxOrder + 1, is_active: true }`,欄位名 `order`/`is_active` 為 snake_case,與後端 System.Text.Json 期望的 `displayOrder`/`isActive` 對不上(JSON.stringify(NaN) 亦會變成 null)。因此後端忽略這些欄位,新部位一律以 DisplayOrder 預設值 0 建立。對照同檔 line 46/211 的種子建立用的是正確的 `displayOrder`(來自 DEFAULT_SECTIONS)與 `isActive`,更凸顯手動新增這條路徑欄位名用錯。
- **重現**:設定頁→機台設定,在既有部位清單下輸入名稱按「新增」數次 → 每個新部位的 displayOrder 都是 0(而非遞增),多筆新增後排序錯亂、堆在最前面,無法穩定排在末尾。
- **影響**:手動新增的機台部位無法取得正確順序值,清單排序錯亂;displayOrder 全為 0 導致後續上下移動與顯示序號不可靠。

### [6] [HIGH] MachineTab 部位排序 API 呼叫參數簽章錯誤,▲▼移動順序永遠寫入失敗
- **區塊/類別**:settings · api-mismatch　**票數**:3✓/0✗
- **位置**:`frontend/src/components/settings/MachineTab.jsx:187`
- **說明**:services/api.js 的 updateMachineSection 簽章為 `(section) => api.put(`/settings/machine-sections/${section.id}`, section)`,只吃「單一物件」並讀 section.id。但 handleMoveSection 以兩個參數呼叫:`apiUpdateSection(currentSection.id, { displayOrder: newCurrentOrder })`(line 187-188)。於是 section = currentSection.id(字串),section.id = undefined,實際送出 `PUT /api/settings/machine-sections/undefined`,request body 變成那串 id 字串而非 payload。後端路由 {id} 為 Guid,'undefined' 解析失敗回 400/404。對照 handleUpdateSectionField(line 238)是正確地以單一物件 `apiUpdateSection(section)` 呼叫,證明 move 這處用錯。
- **重現**:設定→機台設定→選一個部位→按▲或▼。畫面樂觀更新看似有動,但兩個 PUT 都打到 /machine-sections/undefined 失敗(console 出現 'Failed to update section order' 或後端 404)。重新整理頁面後順序回復原狀,順序調整從未真正持久化。
- **影響**:機台部位排序功能完全無法儲存;使用者調整的顯示順序在重載後全部遺失。

### [7] [HIGH] MachineTab 新增部位送出欄位名 order/is_active 與後端 displayOrder/isActive 不符且 maxOrder 為 NaN
- **區塊/類別**:settings · api-mismatch　**票數**:3✓/0✗
- **位置**:`frontend/src/components/settings/MachineTab.jsx:103`
- **說明**:handleAddSection 計算 `maxOrder = Math.max(...machineSettings.sections.map(s => s.order))`(line 97-99),但 section 物件用的是 displayOrder(見 DEFAULT_SECTIONS 與後端 MachineSection.DisplayOrder),沒有 order 欄位,map 全為 undefined → Math.max(undefined...) = NaN。送出的 payload 為 `{ name, order: maxOrder + 1(=NaN), is_active: true }`(line 101-105)。後端 MachineSection 實體(backend/PrintingIoT.Core/Entities/MachineSection.cs)欄位是 DisplayOrder / IsActive,System.Text.Json 對 'order' 與 'is_active' 皆比對不到(is_active 底線與 isActive 不符),故新部位 DisplayOrder 一律落為 0。其餘程式(排序、handleMoveSection、種子資料)全用 displayOrder,唯此處用 order,前後不一致。
- **重現**:設定→機台設定→輸入部位名稱→按「新增」。新增出的部位 DisplayOrder=0,依 `sort((a,b)=>(a.displayOrder||0)-(b.displayOrder||0))` 會排到清單最前面而非最後;連續新增多個部位彼此順序不穩(都是 0)。
- **影響**:手動新增的機台部位排序錯亂(全部擠到最前、順序不定),與『新增後應排在末位』的預期不符。

### [8] [HIGH] 點擊底部 F5+/F6+(良品/不良)按鈕派發非法鍵 'F5+'/'F6+',Dashboard 永不匹配 → 按鈕無作用
- **區塊/類別**:shell · broken-handler　**票數**:3✓/0✗
- **位置**:`frontend/src/components/layout/MainLayout.jsx:509`
- **說明**:footer F 鍵按鈕 onClick 透過 window.dispatchEvent(new KeyboardEvent('keydown', { key: k.code || k.key })) 模擬鍵盤事件。fKeys 陣列中 F5+/F6+ 兩筆只有 key:'F5+' / key:'F6+',沒有 code 欄位(見 MainLayout.jsx:283-284),因此派發出去的 e.key 是字串 'F5+' / 'F6+'。Dashboard 的 handleKeyDown 用 let key = e.key; switch(key) 比對,case 只有 'F5'(Dashboard.jsx:526)與 'F6'(Dashboard.jsx:530),'F5+'/'F6+' 一律落空,switch 不執行任何分支。對比 M/N 那筆有 code:'F8' 所以正常。結果:即時監控畫面上點擊『良品(F5+)』『不良(F6+)』按鈕完全沒反應,resetOffset(良品-1 / 不良+1)不會被調整。實體鍵盤按 F5/F6 因 e.key='F5' 仍正常,只有畫面按鈕壞掉,容易被誤判為偶發。
- **重現**:登入進即時監控(/)頁 → 用滑鼠點擊底部功能列的『F5+ 良品』或『F6+ 不良』按鈕 → 良品/不良計數(resetOffset)無變化、只寫了一條 log;預期應如按實體 F5/F6 一樣調整數量。
- **影響**:現場操作員以觸控/滑鼠點畫面上良品、不良按鈕時完全無效,無法計良品/不良數,屬核心計數功能失效。

### [9] [MEDIUM] handleMoveSection 以 a.order - b.order 排序(欄位不存在→NaN 不排序),但畫面清單依 displayOrder 排序 → 上/下移交換到錯誤的相鄰部位
- **區塊/類別**:api-contract · logic-error　**票數**:3✓/0✗
- **位置**:`frontend/src/components/settings/MachineTab.jsx:159`
- **說明**:line 159 `const sections = [...machineSettings.sections].sort((a, b) => a.order - b.order);` 使用不存在的 `order` 欄位(正確為 displayOrder),`undefined - undefined = NaN`,比較器回傳 NaN 時 Array.sort 視為相等、維持原陣列(抓取)順序,等同沒排序。但實際渲染的清單(line 247/309 sortedSections)是依 `displayOrder` 排序顯示的。因此 handleMoveSection 用 idx±1 找到的「相鄰項」是未依 displayOrder 排序陣列中的鄰居,與使用者在畫面上看到的相鄰項可能不同,導致上/下移交換到非視覺相鄰的部位。
- **重現**:當 machineSettings.sections 陣列順序與 displayOrder 排序不一致時(例如經過新增/刪除後),選中清單中某部位按▲,實際被交換順序的是陣列鄰居而非畫面上方那一列。
- **影響**:部位排序上/下移的目標錯亂,使用者會看到非預期的部位被互換位置。

### [10] [MEDIUM] CommunicationTab 監控更新頻率(monitor_interval)永不持久化
- **區塊/類別**:settings · state-bug　**票數**:2✓/1✗
- **位置**:`frontend/src/components/settings/CommunicationTab.jsx:206`
- **說明**:監控更新頻率的 range/number 綁定 `commSettings.plc.monitor_interval` 並透過 handleCommChange('plc','monitor_interval',...) 更新(line 201-218)。但 handleCommChange 組出的 API payload(line 99-121)只列出 plc_* 固定欄位,完全沒有 monitor_interval;且 mount 時的 fetchSettings(line 44-79)也未從 data 讀回 monitor_interval。因此該值只存在於當下的 React state 與 localStorage 的巢狀結構中,從未送到後端,也不會在重載後被還原(初值 fallback 1.0)。
- **重現**:設定→通訊設定→PLC 選 Wise/MQTT 模式→把『監控更新頻率』拉到 2.0 秒→重新整理頁面。滑桿回到預設 1.0 秒,後端未收到 monitor_interval。
- **影響**:使用者設定的 MQTT 監控輪詢頻率無法儲存,每次重載都被重置為 1.0 秒。

### [11] [MEDIUM] CommunicationTab 測試連線傳入的 host/port 參數被 api.testMqttConnection 全部丟棄
- **區塊/類別**:settings · api-mismatch　**票數**:3✓/0✗
- **位置**:`frontend/src/components/settings/CommunicationTab.jsx:258`
- **說明**:按鈕呼叫 `testMqttConnection({ host, port, device_type, mac_id })`(line 258-263),精心組出使用者輸入的 broker/IP 與 port。但 services/api.js 的 `testMqttConnection = async () => api.post('/simulation/test-mqtt')`(api.js line 93-96)不接受任何參數、也不帶 body,傳入的物件被完全忽略,且打的是固定的 /simulation/test-mqtt 模擬端點。
- **重現**:設定→通訊設定→在 MQTT Broker URL 填入任意主機→按『📡 Test Connection (Backend)』。前端組的 host/port/device_type 全遭丟棄,測試結果與使用者輸入的位址無關(永遠測固定後端)。
- **影響**:測試連線功能無法針對使用者輸入的 MQTT/PLC 位址進行驗證,回報的成功/失敗具誤導性。

### [12] [MEDIUM] i18n 缺鍵:越南文/泰文缺 settings.unit/formula/report,切語系後單位/公式/報表分頁顯示原始 key
- **區塊/類別**:settings · i18n-missing　**票數**:3✓/0✗
- **位置**:`frontend/src/modules/language/LanguageContext.jsx:1031`
- **說明**:實際生效的翻譯來源是 LanguageContext.jsx 內的 translations 物件(locales/index.js 與 locales/tw.js 無任何檔案 import,為未接線的死碼)。translations.vn(line 1000-1033)與 translations.th(line 1034-1067)的 settings 只有 tabs / comm / machine,缺少 unit、formula、report 三組。t() 實作為 `return value || key`(line 1073-1081),缺鍵時直接回傳 key 字串。UnitTab/FormulaTab/ReportTab 大量使用 t('settings.unit.*')、t('settings.formula.*')、t('settings.report.*'),且 LanguageSwitcher 的 availableLanguages 有列出 vn/th 供切換。
- **重現**:右上語系切換至 Tiếng Việt 或 ภาษาไทย→進入設定→單位設定/計算公式/報表設定分頁。標題與欄位直接顯示 'settings.unit.title'、'settings.formula.coreEff'、'settings.report.timeBoundary' 等原始 key,而非翻譯文字。
- **影響**:越南文與泰文使用者在單位、公式、報表三個設定分頁看到未翻譯的程式 key,介面破圖無法閱讀。

### [13] [LOW] GeneralTab 修改使用者時若變更代碼(ID)會新增重複使用者而非更新
- **區塊/類別**:settings · logic-error　**票數**:3✓/0✗
- **位置**:`frontend/src/components/settings/GeneralTab.jsx:87`
- **說明**:handleUserClick 進入編輯狀態時把 newUserCode 設為該列 u.id(line 119)。handleAddUser 以 `existingIdx = users.findIndex(u => u.id === newUserCode)`(line 87)判斷更新或新增。若使用者在編輯過程中改動了『代碼 (ID)』欄位,findIndex 找不到舊 id(existingIdx = -1),就走新增分支 append 一筆新使用者,舊使用者仍保留 → 產生重複資料,但按鈕文字仍是『修改 (Update)』。另 handleUserClick 也未將 username 帶回,更新後 username 會被 handleAddUser 以 newUserName 覆寫。
- **重現**:設定→一般設定→使用者設定→點一列 OP1(代碼 001)→把代碼欄改成 099→按『修改』。結果多出一筆 id=099 的新使用者,原 001 未被移除,清單出現兩筆。
- **影響**:編輯使用者代碼時造成資料重複,而非預期的原地更新。

### [14] [LOW] 速度滑桿左端標籤標為 '0',但滑桿最小值 -1 代表『停止』、0 在中央代表『標準』,標籤誤導
- **區塊/類別**:shell · logic-error　**票數**:2✓/1✗
- **位置**:`frontend/src/components/layout/MainLayout.jsx:373`
- **說明**:速度調整滑桿 input type=range min='-1' max='1'(MainLayout.jsx:375-378),依右側數值顯示邏輯(line 403)speedFactor === -1 顯示『停』、=== 0 顯示『標準』、=== 1 顯示『極速』。但滑桿左端固定文字標籤寫 <span>0</span>(line 373),右端寫『極速』(line 396)。滑桿最左端對應的是 min=-1(停止)而非 0;0(標準)實際落在滑桿正中央。左端標『0』與該端實際語意(停止/-1)不符,會讓操作員誤以為往左拉是回到標準(0)而非停止。
- **重現**:開啟模擬生產(Admin + plcSimulateEnabled)→ 觀察速度滑桿:左端標籤顯示 '0',但把滑桿拉到最左端時右側數值顯示變為『停』(speedFactor=-1),兩者語意矛盾。
- **影響**:速度滑桿端點標示與實際值不一致,操作員可能誤判滑桿刻度,將『停止』誤認為『標準(0)』。

## 4. 反駁淘汰(未達 2 票,存查)

- **[medium] analysis** — 分類欄位 qty / stopReason 對應的紀錄欄位不存在,分組永遠得到 '-'(`AnalysisPage.jsx:37`,1✓/2✗)
- **[low] settings** — FormulaTab 多個數值輸入以字串儲存,與其他欄位 Number() 不一致(`FormulaTab.jsx:64`,0✓/3✗)
- **[medium] shell** — user 為 null 但 showLoginModal 為 true 時繞過守衛,直接存取 user.role/user.name 造成 TypeError 白畫面(`MainLayout.jsx:292`,0✓/3✗)

## 5. 觀察與建議修復序

- **MachineTab 叢集(#4/#5/#7/#8/#12)**:同一檔三個根因 —(a)handleMoveSection 以 (id, 部分物件) 呼叫 updateMachineSection(該 API 只吃單一物件並讀 section.id)→ 打到 /machine-sections/undefined;(b)handleAddSection 用不存在的 s.order(實為 displayOrder)算 maxOrder → NaN,且 payload 欄位 order/is_active 與後端 DisplayOrder/IsActive 不符;(c)排序 a.order-b.order NaN 不排序。建議一次修(與本輪 #5 DI 同檔)。
- **Analysis 叢集(#1/#2/#3)**:#1 critical — 分析頁幾乎所有 i18n 鍵缺失,顯示原始 key;#2 時間刻度聚合 no-op;#3 分類側欄 groupedData 算完沒用。此頁核心功能大面積失效,建議優先。
- **Shell #6(high)**:底部 F5+/F6+ 良品/不良按鈕派發非法鍵字串,滑鼠/觸控點擊無效(實體鍵盤正常)→ 現場計數核心功能。建議修。
- 其餘 #9/#10/#11/#13/#14 為 medium/low,可次批處理。
