# 任務報告 — C′ 遷移三回執行完成(PM/RD/Tester 迴圈)

> 日期:2026-07-07
> 執行方式:`/loop` 動態自排程 + `pm-rd-tester` 工作流,每回 PM→RD→Tester 三層驗收
> 設計依據:`doc/spec20260704-1.md`(方案 C′ 決策)+ `doc/操作說明文件.html`(操作說明書 v3.0)
> 分支:主系統 `feat/extract-maintenance`;MM 為獨立 repo(`/Volumes/G70Pro/cusor pool/MM`)
> 依授權規範:兩 repo 均已 commit、**皆未 push**

---

## 1. 總結

Smart Parts 零件管理(Parts / Suppliers / SupplierParts)已依方案 C′ **完整移出主系統、併入 MM**,
與備品零件(SpareParts)在 MM 內並立為兩元件。P0–P4 全部完成並通過三層驗收;
僅餘 **Cloudflare 儀表板三步驟需 Eric 手動執行**(§4)與 P5 文件對齊(未列入本次三回)。

| 回 | 範圍 | 結果 | 驗收報告 |
|---|---|---|---|
| 回1 | P0 MmsDB 初始 migration + P1 零件管理後端移植 | ✅ PASS(一次過) | MM `tests/report-v1.md` |
| 回2 | P2 資料搬移 + P3 前端整併 | ✅ PASS(額度中斷續跑後一次過) | 主系統 `tests/report-v1.md` |
| 回3 | P4 主系統瘦身 | ✅ PASS(第 2 輪;第 1 輪因 RD 報告缺失退回) | 主系統 `tests/report-v2.md` |

## 2. MM repo 成果(5 個 commit)

- `aeb06e9` **P0**:補 MmsDB 初始 EF migration(InitialCreate,快照 11 表),啟動由 EnsureCreated 改 `MigrateAsync()` + 既有庫 baseline(避免 relation already exists)
- `b38dae0` **P1**:零件管理元件 — Part/Supplier/SupplierPart 實體、PartService/SupplierService、`/api/v1/parts`、`/api/v1/suppliers`、`/api/v1/supplier-parts` 三端點(Controller 取名 PartsCatalogController 避開備品 PartsController)、AddPartsManagement migration、移植 PartServiceTests 6 案 + 新增 Supplier 測試
- `a055bde` **P2**:FlexoDB→MmsDB 三表搬移 — 冪等腳本(前置檢查→pg_dump 備份→單一交易 COPY + ON CONFLICT DO NOTHING→筆數/抽樣核對→來源 md5 佐證)、拋棄式測試庫整合測試 6/6、FlexoDB 全程唯讀;搬移報告 MM `docs/report20260706-1.md`
- `5830934` **P3**:前端整併 — 九頁籤(M2-b):新增「📦 零件主檔」「🏢 供應商」,「零件庫存」更名「🔩 備品庫存」;M13/M14 線框落地;zh/en 雙語;權限 managePartsCatalog/manageSuppliers
- `d6289c2` **附帶**:PartDto 補回 Category/Unit + 新增 GET `/api/v1/parts/{id}`(回2 觀察項修復)

驗收數據:dotnet test 65/65、vitest 89/89、前後端建置與容器建置成功、`/api/parts`(備品)回歸無影響。

## 3. 主系統 repo 成果(4 個 commit)

- `a17f71f` **移除零件管理模組**:三實體、DTOs/Parts、IPartService、PartService、PartsController(api/v1/parts)、PartServiceTests、PrintingContext 三個 DbSet 與設定、DI 註冊;新增 `RemoveSmartPartsModule` migration(**Down 實測可完整還原三表結構**,Tester 親測 Down/Up 可逆)
- `8069abe` **sm-frontend 退役**:docker-compose 移除服務、`smart-parts-frontend/` 整目錄刪除、`backend/SmartParts.API/` 殘留舊專案刪除
- `279f813` **全域 grep 殘留排除**:註解與 README 去零件管理引用(SparePart/MaintenanceParts 等 MM 領域字詞未誤動)
- `add7fde` **RD 實作報告**:`docs/report20260707-1.md` — 筆數對照、migration 三步驟留痕、grep 結果表、tunnel 手動指引

驗收數據:build 0 error、dotnet test 11/11、docker compose config 通過、grep 白名單外零命中。

## 4. ⚠️ 待 Eric 手動執行 — Cloudflare 儀表板三步驟

Tester 查證:`mms.ericchh.work` 目前 **NXDOMAIN**(tunnel 上尚未建立),`smartparts.ericchh.work` 仍回 403。
依主系統 `docs/report20260707-1.md` §6.3:

1. Cloudflare Zero Trust → Tunnels → 對應 tunnel → 新增 public hostname `mms.ericchh.work` → 指向 mms-frontend 容器
2. 移除(或保留)`smartparts.ericchh.work` 原 hostname
3. 設定 `smartparts.ericchh.work` 301 轉導至 `mms.ericchh.work`(Bulk Redirects 或 Redirect Rule)

## 5. 遺留事項

| 項目 | 說明 | 建議 |
|---|---|---|
| P5 文件對齊 | HANDOVER/開發說明書/PROJECT_STATUS/操作說明書 v3.0 定稿 | 另回執行(不在本次三回範圍) |
| `MaintenanceRecord.ScheduleId1` shadow FK 警告 | 回1 Tester 發現之既存瑕疵(MM 保養模型) | 後續回合修 |
| 同請求重複 SupplierName 會建重複供應商 | 主系統原有行為,忠實移植並註解標註 | 後續回合修 |
| 規格筆誤 | spec-v1 AC-4 寫「Cascade FK」實為 Restrict | 下版規格更正 |
| 備份檔無保留上限 | MM `backups/`(已 gitignore) | 視需要清理 |
| 工作流過程檔 | 主系統 `docs/spec-v1.md`、`tests/report-v1/v2.md` 為 PM/Tester 工作檔 | 可留存或併入 P5 整理 |

## 6. 過程備註

- 回2、回3 各經歷一次 session 額度中斷(19:20、00:20 重置),均以 workflow resume 從快取續跑,零重工。
- 回3 第 1 輪 Tester 以 T2 退回 RD 的原因非程式問題,而是 RD 報告缺失 + tunnel 前置不成立未回報 — 分流機制運作正常。
