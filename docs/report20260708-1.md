# 回4 治理缺口修正報告 — J-1 Git 治理缺口處置 + J-2 BLOCKED 復驗接續現況

> 日期:2026-07-08(`date` 取得)
> 撰寫:RD subagent
> 依據規格:`docs/spec-v2.md`(回4 — Git 治理缺口修正 + MM 安全修復後複驗接續)
> 性質:**治理與文件修正回合**,不涉及任何應用程式邏輯修復;主系統 Printing IoT 應用程式碼零異動。

---

## 1. J-1 Git 治理缺口最終處置結果

### 1.1 證據複核(與 `docs/spec-v2.md` §1.1 一致)

| 項目 | 主系統 Printing IoT | MM 保養維修外掛 |
|---|---|---|
| `git remote -v` | `origin` 對應 `eric4577599/Printing-IoT` | `origin` 對應 `eric4577599/MM.git` |
| `git rev-parse HEAD` | `7d82ab6d8f79871f80c7f21f112df070dd38e3b6` | `40091939123e5f2283e0a8b001020f644da031fd` |
| `git rev-parse origin/<branch>` | `origin/feat/extract-maintenance` ≠ HEAD | `origin/main` = HEAD(完全相同) |
| `git status -sb` | `ahead 1` | `main...origin/main`(不顯示 ahead,一致) |
| `git reflog show refs/remotes/origin/<branch>` | 無 `update by push` 紀錄 | 有 `update by push` 紀錄(`4009193`、`30b332e`、`68f0f09`) |
| 判定 | **未 push**,PASS(沿用 v1/v3 標準) | **已實際 push**(CONFIRMED),構成治理缺口 J-1 |

`update by push` 為本機對遠端執行 `push` 後 reflog 留下的紀錄,非單純 `fetch` 追上遠端既有狀態可產生 — 判定為 CONFIRMED,非誤判。

### 1.2 升級路徑執行狀況

1. **未對 MM repo 執行任何回退性操作**(`reset --hard`、`push --force` 等)— 遵循規格禁止事項,保留現狀。
2. **TeamChat 治理缺口通知**:依 `docs/spec-v2.md` §1.2-2 應向 Eric 發送 `incident` 頻道通知(Dual-Mention `@Eric @Eric-LT AI`)。**本輪 RD subagent 執行環境無 TeamChat 外部網路存取權限**(sandbox 未開放 `team-chat.corriot.com` 連線,且本機 `bots/eric/teamchat.py` 之 `WORKSPACE` 常數為 `team-infotech` 而非規格要求之 `team-iot`,需先由具備正確 workspace API key 與網路權限的執行環境 — 如 eric-pod 常駐秘書 — 補送),**通知尚未實際送出**,已記錄於 `handoff.md`「2026-07-08 Git 治理缺口(J-1)通知與裁示」段落,待補送。
3. **Eric 裁示**:因通知尚未送達,**待 Eric 裁示**(暫記於 `handoff.md`,回覆後依 §5 E11/E12 分流處置並回補)。
4. **文件更正(不論裁示結果皆須完成之項目,本輪已全數完成)**:
   - `handoff.md`:「兩 repo 全部 commit...皆未 push」句已更正為反映實況(MM 已 push、主系統未 push),並附證據摘要與「待 Eric 裁示」暫記。
   - `docs/report20260707-4.md`:末尾新增「事後補述(2026-07-08)」段落,說明 §4.2 原「全數未推」描述已由本回治理缺口修正,原表格判定文字未刪改(維持歷史留痕)。
   - `README.md:9`:Cloudflare 狀態措辭更新,移除已過時的「待 Eric 手動執行」,改為反映 2026-07-07 已生效上線、2026-07-08 因 J-2 安全事件暫時 502 之現況。

### 1.3 AC 判定

| AC | 判定 | 說明 |
|---|---|---|
| AC-C1(v2).1 主系統未 push | **PASS** | `ahead 1`、無 push reflog,沿用 v1/v3 標準 |
| AC-C1(v2).2 MM 實況與文件記載一致 | **PASS**(本回文件修正後) | `handoff.md`、`docs/report20260707-4.md` 已更正為與 `git rev-parse` 實況一致 |
| AC-C1(v2).3 Eric 裁示已記錄 | **部分**(暫記「待 Eric 裁示」) | 通知尚未送達 Eric,無法記錄實際裁示結果;已依規格允許之「待 Eric 裁示」暫記處理,待補送通知並取得回覆後回補 |
| AC-C3 README 措辭與現況無矛盾 | **PASS** | 見 §1.2-4 |

---

## 2. J-2 BLOCKED 項目復驗現況

依 `docs/spec-v2.md` §2.1,觸發條件(1. MM 安全修復完工並經 Tester PASS;2. Eric 明確確認重新對外開放 MM 三容器)**均尚未成立**:

- MM repo 現有 `docs/spec20260708-1.md`(安全修復規格)、`docker-compose.yml` 修改中、新增 `.env.example`,顯示安全修復工作流**進行中、尚未結案**。
- 無 Eric 明確確認重新對外開放三容器之紀錄。

依規格 E14「觸發條件尚未成立,但被要求先跑跑看 → 拒絕執行」,以下五項**維持 BLOCKED**,本輪未執行、不得標 PASS:

| AC | 判定 |
|---|---|
| AC-B3-1(復驗) | ☐ 待觸發(BLOCKED) |
| AC-B3-2(復驗) | ☐ 待觸發(BLOCKED) |
| AC-B4(復驗) | ☐ 待觸發(BLOCKED) |
| AC-B5(復驗) | ☐ 待觸發(BLOCKED) |
| AC-B6(復驗) | ☐ 待觸發(BLOCKED) |

---

## 3. 兩 repo 最終 commit 清單

### 3.1 主系統 Printing IoT(`feat/extract-maintenance`,自 `b683831` 起 11 筆,`origin/feat/extract-maintenance` ahead 1,未 push)

```
7d82ab6 feat(schedule): 點選產品庫列彈出唯讀規格詳情視窗 — 基本資料/機械參數/印刷部/展開圖
c0d7743 docs: handoff 更新 — Cloudflare 路由完成,C′ 遷移全案收尾
f5a8d77 docs: 回3 回歸驗證工作檔 — spec-v3(AC-B5 修訂)與各輪 Tester 報告
ae2a4d7 docs: 回3 最終回歸報告 — AC-B5 依 spec-v3 標準重新判定 PASS
3008939 fix(frontend): DocsPortal 移除已遷出模組之會議紀錄索引項
633c0ba fix(compose): 改寫歷史註解移除 Smart Parts 殘留關鍵字
c5d58ea test(frontend): 補 BoxDiagram 面數回歸測試(RSC/HSC 4 面、S5 有值 5 面、尺寸欄對齊)
8515178 docs: P5 文件對齊回合 Tester 驗收報告
284baa7 fix(dashboard): 移除排程面板未翻譯的訂單操作按鈕列;展開圖修正多一面問題
97f359d docs: P5 文件對齊 — 主系統文件與操作說明書 v3.0 定稿
5a70efe docs: C′ 遷移工作檔納入版控
```

另加:本輪回4治理缺口文件修正(`handoff.md`、`docs/report20260707-4.md`、`README.md`、本報告),以 `docs:` 前綴獨立 commit,提交後之 HEAD 仍維持未 push。

工作區另有 `?? docs/spec-v2.md`(PM 產出之本回依據規格,workflow 管理檔,RD 未動)。

### 3.2 MM 保養維修外掛(`main`,自 `e3cebd8` 起 11 筆,HEAD = `origin/main`,**已 push**,J-1 治理缺口對象)

```
4009193 fix(scripts): migrate-parts-data --verify 於來源三表移除後改以最新備份檔對照
c0617ee docs(compose): 更正 mms-backend 過時註解 EnsureCreated → MigrateAsync
1f2d760 feat(scripts): migrate-parts-data.sh 備份檔保留上限 BACKUP_KEEP(預設 5),含清理測試
a3066f1 fix(parts): 同請求重複 SupplierName 僅建一筆供應商(請求內去重),補單元測試
94e56f3 fix(ef): 綁定 MaintenanceRecord 導覽屬性關聯,消除 ScheduleId1/PartId1 shadow FK,並以 migration 清除多餘欄位
1ed1591 docs: P5 文件對齊 — README 兩元件定位改寫與 CLAUDE.md 新建
d6289c2 fix(parts): PartDto 補回 Category/Unit 欄位 + 新增 GET /api/v1/parts/{id}
5830934 feat(p3): 前端整併零件管理 — 📦零件主檔/🏢供應商頁籤 + 🔩備品庫存更名(M2-b 九頁籤)
a055bde feat(p2): FlexoDB→MmsDB 零件三表資料搬移 — 冪等腳本 + 整合測試 + 搬移報告
b38dae0 feat(parts): 移植零件管理元件 — Part/Supplier/SupplierPart + /api/v1 端點
aeb06e9 feat(db): 補 MmsDB 初始 EF migration,啟動改 MigrateAsync + 既有庫 baseline
```

MM repo 工作區另有 `M docker-compose.yml`、`?? .env.example`、`?? docs/spec20260708-1.md`(安全修復工作流進行中,獨立於本檔範圍,不予處理)。

---

## 4. 遺留事項

1. **TeamChat 治理缺口通知尚未送出**:需具備 team-iot workspace API key 與網路存取權限之執行環境補送 `incident` 頻道通知,待 Eric 裁示 J-1 為 (a) 已授權例外或 (b) 未經授權流程缺口,並依 E11/E12 回補 `handoff.md` 與本報告 §1.3 AC-C1(v2).3。
2. **AC-C1(v2) 最終判定尚未完全 PASS**:文件一致性(1.2)已達成,但裁示記錄(1.3)因通知未送出而暫記,待後續補齊。
3. **J-2 五項復驗維持 BLOCKED**:MM 安全修復工作流(`MM/docs/spec20260708-1.md`)仍在進行中(工作區有未提交變更),且無 Eric 明確重啟確認;觸發條件成立後由 Tester 依 `docs/spec-v2.md` §2.2 逐項復驗,不在本輪處理範圍。
4. 主系統 Printing IoT 應用程式碼本輪零異動,確認不受 J-1、J-2 影響。
