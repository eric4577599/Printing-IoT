# 任務報告 — 語言切換鈕 + 全站 i18n 同步(依 workflow 分工,處理三回)

> 日期:2026-07-26(`date` 取得)
> 撰寫:Claude(PM/RD/Tester 手動分工)
> 版次:report20260726-4
> 需求:導覽列增加「語言」切換鈕;每個內頁檢查可同步更換語言。以 workflow 分工處理三回。

---

## 1. 摘要

新增導覽列「🌐 語言」下拉切換鈕;並用 3 個平行審計 agent 盤點全站硬編碼字串,分三回把**最高可見度**的介面接入 i18n,使切換語言時同步更換。

**重要規模發現:全站約 500 個畫面可見、未走 `t()` 的硬編碼中文字串**(多為 `中文 (English)` 雙語硬編碼)。本回三回合聚焦最高可見度介面完成上線,其餘以審計精確 backlog(file:line→key)交付後續。

> 分工方式:**未用 `pm-rd-tester` 命名工作流**(已知會誤讀本 repo 殘留 `spec-v*.md`,見 handoff / memory `rd-qc-relay-workmode`),改手動分工 + 平行 Explore/developer subagent。

## 2. 三回合成果

### Round 1 — 語言鈕 + 導覽列 i18n
- **新增 `LanguageNavMenu.jsx`**:導覽列「🌐 語言 ▾」下拉(5 語),選任一即時同步切換全站(共用 `LanguageContext`)。置於 nav「文件」之後(對應需求圖示紅圈)。
- 導覽列標籤由硬編碼 `即時監控 (Monitor)` 改 `t('nav.*')`;五語補 `nav.analysis/docs/language`。
- F9`班別`/F10`退回` 改 `t('fkeys.f9/f10')`。

### Round 2 — 最高可見度介面(每頁都在)
- **LoginModal 班別選擇視窗**(每次載入必現,26 處):加 `useLanguage`,全接 `t('login.*')`(subagent 執行,ESLint 綠)。
- **MainLayout 漏接**(13 處):模擬控制列、狀態列(PLC/ERP 連線)、登出、說明按鈕 → `t('layout.*')`。
- **BoxDiagram**(4 處):空狀態、尺寸單位、盒型標示 → `t('boxDiagram.*')`。
- 五語新增 `login.*` / `layout.*` / `boxDiagram.*`。

### Round 3 — 生產報表頁群
- **Reports 四檔**(ReportsPage / DailyReportView / MonthlyReportView / StopReasonView,共 **117 處**):整檔加 `useLanguage`,全接 `t('reportView.*')`(subagent 執行)。
- 五語新增 `reportView.*`(96 鍵)。
- 修復回歸:BoxDiagram 改用 `useLanguage` 後,`BoxDiagram.test.jsx` 需包 `LanguageProvider`(已修 render 包裝)。

## 3. 變更檔案

| 檔案 | 變更 |
|---|---|
| `modules/language/LanguageNavMenu.jsx` | **新增** 導覽列語言下拉 |
| `modules/language/LanguageContext.jsx` | 五語新增 nav.analysis/docs/language、`layout.*`、`boxDiagram.*`、`login.*`、`reportView.*` |
| `components/layout/MainLayout.jsx` | nav 標籤 i18n、掛 LanguageNavMenu、F9/F10、sim/status/logout/help 接 t() |
| `modules/auth/LoginModal.jsx` | 班別視窗 26 處接 t() |
| `components/common/BoxDiagram.jsx` | 4 處接 t() |
| `modules/reports/*.jsx`(4) | 117 處接 t() |
| `pages/analysis/AnalysisPage.jsx` | (前一任務)controls 已接 t() |
| `tests/i18n/localeParity.test.js` | 既有:五語鍵集合對等守門 |
| `tests/i18n/reportViewKeys.test.js` | **新增** reportView 缺鍵回歸 |
| `tests/components/BoxDiagram.test.jsx` | 修:render 包 LanguageProvider |

## 4. 測試與建置

- `localeParity`:cn/en/vn/th 鍵集合與 tw **完全一致 + 無空值**(涵蓋所有新增命名空間)。
- 全套 vitest **91 passed**(排除環境壞掉的 e2e);`vite build` ✓。

## 5. 驗證(線上)

- 重建 `printingiot-frontend-1` 上線。
- 瀏覽器實測(2026-07-26):**PASS**。
  - 導覽列出現 **🌐語言 ▾** 下拉鈕(在「文件」之後,對應需求圖),點開列 5 語,選任一即時同步。
  - 切泰文後同步更換:nav(จอภาพ/กำหนดการ/รายงาน/วิเคราะห์/การตั้งค่า/เอกสาร/ภาษา)、狀態列(สถานะ / PLC เชื่อมต่อ / ERP เชื่อมต่อ)、說明(ช่วยเหลือ)、登出(ออกจากระบบ)、**Reports 頁**(側欄 ประเภทรายงาน + 4 分頁、控制列、13 欄表頭、空狀態)全泰文,無 `nav.xxx` 點路徑、無殘留中文標籤。
  - 殘留中文皆為資料(客戶 QA測試、產品名、機台部位設定名),非 i18n 標籤。
- 已知限制:語言無 localStorage 持久化,重整回預設 tw(班別視窗故以 tw 顯示)—— 屬 S3 Phase 2 選配。

## 6. 尚未完成 backlog(審計已精確映射,file:line→key)

全站硬編碼盤點(3 agent):**Schedule/Dashboard/Modal 110、Reports/Docs/共用/班別 193、Settings 198**。本回完成最高可見度部分;**未完成約 340 字串**,清單存於 scratchpad `loop-lang-audit-{A,B,C}.md`:

| 區塊 | 量 | 說明 |
|---|---|---|
| Settings 7 tab | 198 | BoxType/General/Communication/Formula/Machine/Report/Unit,皆雙語硬編碼;多映射 settings.common.* 共用鍵 |
| Modal params / MaintenancePage / ProductFormModal / AddScheduleModal | ~110 | 印後參數名(modals.params.*)、四支整檔未接 i18n;MaintenancePage 幾乎是 Schedule 未 i18n 鏡像,多數映射既有鍵免翻譯 |
| DocsPortal | 31 | 文件清單名稱與入口文案 |

> 建議後續:優先 Settings(使用者常進);多數項目映射既有鍵或 settings.common.*,實作以「接 t()」為主、翻譯量較集中在 params/Settings 標題。

## 7. 註記

- 語言碼沿用既有 `vn`(非 ISO `vi`)。
- 殘留中文屬「資料值」(客戶/產品/機台部位/停機原因種子)非 UI 標籤者,不動。
- 匯出檔(Excel/PNG/CSV)欄名/檔名在地化列選配。
- 未 `git push`(全域紅線)。
